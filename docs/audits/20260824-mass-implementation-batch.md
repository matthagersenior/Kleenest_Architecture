# Mass implementation batch — 2026-08-24

## User-observed failures reconciled

The deployed screenshots exposed three product failures plus a structural deployment/runtime problem:

1. Map showed ingested locations instead of consistently propagating fresh user-location discovery.
2. Route build failed at `live_network_events` RLS publication.
3. Play was reachable but its progression service contract was incomplete.
4. The deployed UI did not reflect the canonical runtime tree reliably. The repository had a duplicate root `src/CanonicalAppRuntime.jsx` whose imports referenced components that actually live under `src/runtime/`. The root entry is now a one-line delegate to `src/runtime/CanonicalAppRuntime.jsx`, eliminating that duplicate router boundary.

## Implemented

### Maps

- GPS-first startup discovery.
- External nearby discovery runs before the canonical nearby read.
- Universal discovery bootstrap follows the same path.
- Location denial is surfaced instead of silently treating a fallback city as the user's current area.

### Routing

- Real route geometry is now generated by the routing service using canonical location coordinates where available.
- Address/coordinate fallback geocoding is supported.
- Distance, duration, GeoJSON geometry, and turn-by-turn steps are returned to the route UI.
- Anonymous users can build route geometry; authenticated users publish lifecycle events through the protected live-network contract.
- Route UI exposes Start, Approaching, Arrived, and Departed lifecycle actions.

### Play

- Added an interactive game center with six short games: Clean Sweep, Bathroom Memory, Kleenest Quick Quiz, Bathroom Word Mix, Flush the Facts, and Clean Grid.
- Game scores and duration terminate at the authoritative `record_game_result` RPC.
- Production verification confirms `record_game_result` is security-definer, executable by `authenticated`, and not executable by `anon`.
- `/games` now reaches the actual game center rather than the generic progression page.

### Workspace UX

- Membership-aware quick actions are surfaced for Consumer, Business, Fleet, Enterprise, and Owner Control.
- Owner Platform CRUD is a first-class highlighted action.
- Workspace presentation receives role-specific visual hierarchy without requiring JSON for normal operations.

### Enterprise

- `get_enterprise_partner_network` production boundary is owner/admin + Fleet/Enterprise scoped while preserving its exact return contract.
- `create_partner_allocation` follows the same network ownership/tier boundary.
- `get_partner_network_benchmark` was additionally hardened in Production with the same owner/admin + Fleet/Enterprise network boundary and recorded in a new migration.

### Fleet

- `fleet_metric_source_allowed(text,text)` remains internally protected because direct privilege closure is blocked by the platform safety layer; the repository records the queued state rather than falsely claiming completion.

## Deployment root-cause finding

The canonical application entry imported `src/CanonicalAppRuntime.jsx`, but that root file contained an obsolete router importing pages from the wrong directory. The actual implemented runtime is under `src/runtime/`. This created a second router boundary and was capable of leaving Pages on stale or inconsistent UI behavior even while runtime files were being improved.

The root file now delegates directly to the runtime router. This is an architectural correction, not a cosmetic workaround.

## Acceptance standard

The next verification pass must test the deployed build at:

`button/control → route → domain service → Supabase RPC/table → authoritative state refresh → telemetry`

for Maps, Route, Play, Owner CRUD, Business, Fleet, and Enterprise. A capability is not considered complete merely because its page renders.
