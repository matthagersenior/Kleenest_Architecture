# Consumer map acceptance

A first-time consumer should be able to open the map, identify bathroom pins immediately, select one, and see its identity plus the three primary actions: Navigate, Add to Route, Details.

Secondary intelligence belongs in the selected preview or details page. Empty engagement modules must remain hidden.

The same place identity and status must be used by map pin, result card, popup, and details.
