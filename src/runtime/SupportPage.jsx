import {useState} from 'react';
import {useAppContext} from '../AppContext.jsx';
import WorkspaceShell from './WorkspaceShell.jsx';
export default function SupportPage(){const {services}=useAppContext();const [subject,setSubject]=useState('');const [message,setMessage]=useState('');const [category,setCategory]=useState('general');const [status,setStatus]=useState('');const submit=async()=>{try{setStatus('Sending…');await services.support.create({subject,message,category});setSubject('');setMessage('');setStatus('Support request submitted.')}catch(e){setStatus(e.message)}};return <WorkspaceShell><section className="page-head"><span className="eyebrow">Support</span><h1>How can we help?</h1><p>Report a bug, request a feature, or ask a question.</p><label>Category<select value={category} onChange={e=>setCategory(e.target.value)}><option value="general">General</option><option value="bug">Bug</option><option value="feature">Feature request</option><option value="account">Account</option><option value="billing">Billing</option></select></label><label>Subject<input value={subject} onChange={e=>setSubject(e.target.value)} /></label><label>Message<textarea value={message} onChange={e=>setMessage(e.target.value)} /></label><button className="button primary" disabled={!subject.trim()||!message.trim()} onClick={submit}>Send support request</button><span>{status}</span></section></WorkspaceShell>}
