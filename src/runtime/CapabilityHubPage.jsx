import { useEffect, useMemo, useState } from 'react';
import { ArrowRight, MapPin, Route, ShieldCheck, Trophy, QrCode, Radio, Building2, BarChart3, Truck, Globe2, Bell, Settings, Search, AlertTriangle, Database, Server } from 'lucide-react';
import { Link } from 'react-router-dom';
import { useAppContext } from '../AppContext.jsx';
import { auditCapabilitySurface } from '../architecture/capabilityContract.js';
import WorkspaceShell from './WorkspaceShell.jsx';
import './CapabilityHubPage.css';

const groups=[
 {title:'Bathroom interaction',icon:MapPin,items:[['Map','/map','Discover canonical locations, map intelligence and live availability.',MapPin],['Routing + offline','/route','Plan multi-stop restroom routes and preserve activity when connectivity drops.',Route],['Check-in','/activity','Turn real visits into verified trust signals.',ShieldCheck],['Review + evidence','/evidence','Contribute ratings, observations, photos and provenance.',ShieldCheck],['Engagement','/engage','Connect verified activity to progression and rewards.',Trophy]]},
 {title:'Engagement primitives',icon:Trophy,items:[['QR','/engage','Use QR as an attribution and action trigger.',QrCode],['Geofence','/engagement/orchestrate','Use proximity events as contextual triggers.',Radio],['Quest','/play/quest','Run predefined Trust Quests using normal Kleenest activity.',Trophy],['Leaderboards','/leaderboards','Turn measurable participation into recognition and rewards.',Trophy]]},
 {title:'Commercial operations',icon:Building2,items:[['Business','/business','Manage locations, campaigns, promotions and engagement.',Building2,'business_workspace'],['Business intelligence','/business/intelligence','Turn trust, occupancy and engagement signals into decisions.',BarChart3,'business_intelligence'],['Fleet','/fleet','Coordinate routes, vehicles, drivers and operational performance.',Truck,'fleet_workspace'],['Enterprise','/enterprise','Coordinate partner networks, campaigns and shared outcomes.',Globe2,'enterprise_workspace']]},
 {title:'Platform intelligence',icon:Settings,items:[['Cross-tier intelligence','/intelligence','Shared network signals and leaderboards.',Globe2],['Notifications','/notifications','Delivery, priority and preference controls.',Bell],['Admin','/admin','Platform command, integrity and governance.',Settings,'admin'],['Owner','/owner','Owner controls, tier previews and audit surfaces.',Settings,'owner']]}
];
const aliases={business:'business',fleet:'fleet',enterprise:'enterprise',owner:'owner',admin:'admin',consumer:'consumer'};
const hasWorkspaceCapability=(workspace,code,visible,catalog)=>{if(!code)return true;if(workspace==='owner'||workspace==='admin')return true;const normalized=aliases[workspace]||'consumer';const visibleCodes=new Set((visible||[]).map(v=>typeof v==='string'?v:v?.code||v?.feature_code||v?.key).filter(Boolean));if(visibleCodes.has(code))return true;if(catalog?.length){const row=catalog.find(v=>v.feature_code===code);return !!row?.enabled&&String(row.minimum_tier||'').toLowerCase()===normalized||!!row?.enabled&&String(row.minimum_tier||'').toLowerCase()==='free'&&normalized==='consumer'}return false};
export default function CapabilityHubPage(){const{capabilityRegistry,workspaceCapabilities,membershipTier,configured,services,activeWorkspace}=useAppContext();const[query,setQuery]=useState('');const[catalog,setCatalog]=useState([]);const[contracts,setContracts]=useState([]);const workspace=activeWorkspace||(['business','fleet','enterprise','owner','admin'].includes(String(membershipTier||'').toLowerCase())?String(membershipTier).toLowerCase():'consumer');useEffect(()=>{let active=true;if(!configured||!services?.capabilityCoverage)return undefined;Promise.all([services.capabilityCoverage.list(),services.capabilityCoverage.catalog?.(),services.capabilityCoverage.contracts?.()]).then(([coverage,cat,contractsData])=>{if(!active)return;setCatalog(Array.isArray(cat)?cat:(Array.isArray(coverage)?coverage:[]));setContracts(Array.isArray(contractsData)?contractsData:[])}).catch(()=>{if(active){setCatalog([]);setContracts([])}});return()=>{active=false}},[configured,services]);const capabilities=useMemo(()=>Object.entries(capabilityRegistry||{}).map(([key,item])=>({key,...item,name:item?.name||key,domain:item?.domain||key,description:item?.description||`Canonical ${key} capability`,status:item?.status})).filter(item=>{const q=query.trim().toLowerCase();return !q||`${item.name} ${item.domain} ${item.description||''}`.toLowerCase().includes(q)}),[capabilityRegistry,query]);const runtimeAudit=useMemo(()=>auditCapabilitySurface({workspace,services:services||{},visibleCapabilities:workspaceCapabilities||[],catalog}),[workspace,services,workspaceCapabilities,catalog]);const supabaseFiltered=contracts.filter(c=>{const q=query.trim().toLowerCase();return !q||`${c.domain} ${c.canonical_capability} ${c.canonical_rpc} ${c.owner_surface}`.toLowerCase().includes(q)});const total=groups.reduce((n,g)=>n+g.items.length,0);return <WorkspaceShell workspace={workspace}><main className="page capability-hub"><div className="page-header"><div><span className="eyebrow">KLEENEST PLATFORM</span><h1>Everything Kleenest can do.</h1><p>One human-facing map of the product's connected capabilities, including the live Supabase capability contracts behind them.</p></div><div className="hero-actions"><span className="membership-badge">{membershipTier==='premium'?'Premium · ad-free':'Free · full access + ads'}</span><Link className="button secondary" to="/integration">Integration health <ArrowRight size={15}/></Link></div></div><section className="detail-panel capability-runtime"><div><span className="eyebrow">RUNTIME STATUS</span><h2>{configured?'Connected to Kleenest services':'Connection needs attention'}</h2><p>{configured?`Authoritative services are available through the ${workspace} workspace.`:'Supabase is not currently configured for this runtime.'}</p></div><div className="runtime-stats"><div><strong>{total}</strong><span>primary pathways</span></div><div><strong>{capabilities.length}</strong><span>registered UI capabilities</span></div><div><strong>{contracts.length}</strong><span>Supabase contracts</span></div><div><strong>{runtimeAudit.complete}</strong><span>fully reconciled</span></div></div></section>{(runtimeAudit.missingServices.length||runtimeAudit.hidden.length||runtimeAudit.disabled.length)?<section className="detail-panel capability-runtime"><div><span className="eyebrow"><AlertTriangle size={14}/> CONTRACT AUDIT</span><h2>{runtimeAudit.missingServices.length+runtimeAudit.hidden.length+runtimeAudit.disabled.length} capability contract gap{runtimeAudit.missingServices.length+runtimeAudit.hidden.length+runtimeAudit.disabled.length===1?'':'s'}</h2><p>{runtimeAudit.missingServices.length} missing runtime service coverage; {runtimeAudit.hidden.length} not exposed to the current workspace; {runtimeAudit.disabled.length} disabled in the live catalog.</p></div></section>:<section className="detail-panel capability-runtime"><div><span className="eyebrow"><ShieldCheck size={14}/> CONTRACT AUDIT</span><h2>Capability contract reconciled</h2><p>The UI, runtime services, workspace exposure, and live feature catalog agree for the audited surface.</p></div></section>}<section className="detail-panel capability-search"><Search size={18}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search capabilities, outcomes, Supabase RPCs, or domains…" aria-label="Search capabilities"/></section>{groups.map(group=>{const GroupIcon=group.icon;const items=group.items.filter(x=>{const q=query.trim().toLowerCase();if(q&&!`${x[0]} ${x[2]}`.toLowerCase().includes(q))return false;return hasWorkspaceCapability(workspace,x[4],workspaceCapabilities,catalog)});if(!items.length)return null;return <section className="capability-section" key={group.title}><div className="section-heading"><div><span className="eyebrow"><GroupIcon size={14}/> CAPABILITY CLUSTER</span><h2>{group.title}</h2></div><span className="tag">{items.length} pathways</span></div><div className="capability-grid">{items.map(([label,href,description,Icon])=><Link className="capability-card" to={href} key={label}><span className="capability-card-icon"><Icon size={20}/></span><span><strong>{label}</strong><em>{description}</em></span><ArrowRight size={17}/></Link>)}</div></section>})}<section className="capability-section"><div className="section-heading"><div><span className="eyebrow"><Server size={14}/> SUPABASE CAPABILITY CONTRACTS</span><h2>Authoritative backend capabilities</h2><p>These are the live capability contracts exposed by Supabase. They are shown here so the UI represents what the backend actually owns.</p></div><span className="tag">{supabaseFiltered.length} active contracts</span></div><div className="capability-grid">{supabaseFiltered.length?supabaseFiltered.map(item=><div className="capability-card passive" key={item.domain}><span className="capability-card-icon"><Database size={19}/></span><span><strong>{item.canonical_capability}</strong><em>{item.domain} · RPC: {item.canonical_rpc} · surface: {item.owner_surface}</em></span><span className="tag">ACTIVE</span></div>):<div className="capability-card passive"><span className="capability-card-icon"><Database size={19}/></span><span><strong>No matching Supabase contracts</strong><em>Try a different capability or RPC search.</em></span></div>}</div></section><section className="capability-section"><div className="section-heading"><div><span className="eyebrow">FEATURE CATALOG</span><h2>Live Supabase feature catalog</h2></div><span className="tag">{catalog.length} features</span></div><div className="capability-grid">{catalog.filter(item=>{const q=query.trim().toLowerCase();return !q||`${item.feature_code} ${item.name} ${item.category} ${item.minimum_tier}`.toLowerCase().includes(q)}).map(item=><div className="capability-card passive" key={item.feature_code}><span className="capability-card-icon">{item.enabled?<ShieldCheck size={19}/>:<AlertTriangle size={19}/>}</span><span><strong>{item.name}</strong><em>{item.feature_code} · {item.category} · minimum tier: {item.minimum_tier}</em></span><span className="tag">{item.enabled?'ENABLED':'DISABLED'}</span></div>)}</div></section><footer className="home-footer"><span>Free users receive the complete consumer product. Premium removes advertising; it does not remove features.</span><Link className="text-link" to="/pricing">Membership details <ArrowRight size={14}/></Link></footer></main></WorkspaceShell>}
