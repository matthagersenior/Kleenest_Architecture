import{useEffect,useState}from'react';import{Link}from'react-router-dom';import{RefreshCw,Trophy,Building2,Truck,Users}from'lucide-react';import{useAppContext}from'../AppContext.jsx';import WorkspaceShell from'./WorkspaceShell.jsx';
const displayValue=(value,fallback='—')=>{if(value===undefined||value===null||value==='')return fallback;if(Array.isArray(value))return value.length?`${value.length} items`:fallback;if(typeof value==='object')return String(value.name||value.display_name||value.title||value.label||value.code||value.status||value.value||'Updated');return String(value)};
function Board({title,icon:Icon,rows,metric,error}){return <section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">{metric}</span><h2>{title}</h2></div><Icon size={20}/></div>{error?<p className="form-error" role="alert">{error}</p>:!rows?.length?<p>No ranked activity is available yet.</p>:<div className="stack-list">{rows.map((row,i)=>{const name=displayValue(row?.name??row?.display_name??row?.actor_name??row?.business_name??row?.email,'Participant');const value=displayValue(row?.metric_value??row?.value??row?.score??row?.points);return <div className="list-row" key={row?.id||row?.actor_id||row?.business_id||i}><strong>#{i+1} {name}</strong><span>{value}</span></div>})}</div>}</section>}
export default function NetworkIntelligenceSurface(){const{services,user}=useAppContext();const[data,setData]=useState({cross:[],platform:[],business:[],fleet:[]});const[errors,setErrors]=useState({});const[loading,setLoading]=useState(true);async function load(){if(!services?.intelligence){setLoading(false);return}setLoading(true);setErrors({});const specs=[['cross','consumer participation',()=>services.intelligence.crossTierLeaderboard('consumer_checkins',10)],['platform','platform progression',()=>services.intelligence.platformLeaderboard('users:points',10)],['business','business activity',()=>services.intelligence.businessLeaderboard('check_ins',10)],['fleet','fleet network',()=>services.intelligence.fleetNetworkLeaderboard('stops_completed',10)]];const results=await Promise.all(specs.map(async([key,label,fn])=>{try{return[key,await fn(),null]}catch(e){return[key,[],`${label}: ${e?.message||String(e)}`]}}));const next={};const nextErrors={};for(const[key,value,error]of results){next[key]=Array.isArray(value)?value:[];if(error)nextErrors[key]=error}setData(next);setErrors(nextErrors);setLoading(false)}useEffect(()=>{if(user)void load();else setLoading(false)},[user?.id]);return <WorkspaceShell><section className="page"><div className="page-header"><div><span className="eyebrow">NETWORK INTELLIGENCE</span><h1>Signals that connect the network</h1><p>Production leaderboards across participation, business activity and fleet performance.</p></div><button className="secondary" onClick={load} disabled={loading||!user}><RefreshCw size={16}/> Refresh</button></div>{!user?<p className="state">Sign in to view network intelligence.</p>:<><div className="hero-actions"><Link className="secondary" to="/play"><Trophy size={16}/> Progression</Link><Link className="secondary" to="/business/analytics"><Building2 size={16}/> Business analytics</Link><Link className="secondary" to="/fleet/intelligence"><Truck size={16}/> Fleet intelligence</Link></div>{loading?<div className="detail-panel"><p>Loading network intelligence…</p></div>:<div className="detail-grid"><Board title="Cross-tier participation" icon={Users} rows={data.cross} error={errors.cross} metric="CONSUMER CHECK-INS"/><Board title="Platform progression" icon={Trophy} rows={data.platform} error={errors.platform} metric="USER POINTS"/><Board title="Business activity" icon={Building2} rows={data.business} error={errors.business} metric="CHECK-INS"/><Board title="Fleet network" icon={Truck} rows={data.fleet} error={errors.fleet} metric="STOPS COMPLETED"/></div>}</>}</section></WorkspaceShell>}