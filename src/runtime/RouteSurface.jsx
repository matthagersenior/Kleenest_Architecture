import { useEffect, useState } from 'react';
import { Navigation, LocateFixed, MapPin, Play, CheckCircle2, Clock3, Route as RouteIcon, Sparkles, Plus, Trash2, Trophy, ExternalLink, GripVertical, AlertTriangle } from 'lucide-react';
import { useSearchParams, Link } from 'react-router-dom';
import WorkspaceShell from './WorkspaceShell.jsx';
import { useAppContext } from '../AppContext.jsx';
import './RouteSurface.css';

const DRAFT_KEY = 'kleenest.routeDraft.v1';
const readDraft = () => { try { const v = JSON.parse(localStorage.getItem(DRAFT_KEY) || 'null'); return v && typeof v === 'object' ? v : null; } catch { return null; } };
const writeDraft = draft => { try { localStorage.setItem(DRAFT_KEY, JSON.stringify(draft)); } catch {} };
const formatDistance = r => r?.distanceKm != null ? `${Number(r.distanceKm).toFixed(1)} km` : r?.distanceMeters != null ? `${(r.distanceMeters / 1609.344).toFixed(1)} mi` : '—';
const formatDuration = r => r?.durationMinutes != null ? `${Math.round(r.durationMinutes)} min` : r?.durationSeconds != null ? `${Math.max(1, Math.round(r.durationSeconds / 60))} min` : '—';
const destinationLabel = (route, destination, stopName) => route?.destination || destination || stopName || (route?.locationId ? 'Selected restroom' : 'Destination');
const asSnapshot = (value, fallbackId = '') => { if (!value) return null; if (typeof value === 'string') return { locationId: value }; return { locationId: String(value.locationId || value.id || fallbackId), name: value.name || value.brand || 'Restroom stop', address: value.address || [value.city, value.state].filter(Boolean).join(', '), latitude: value.latitude, longitude: value.longitude, bathroomStatus: value.bathroomStatus || value.bathroom_status || value.bathroom?.status, bathroomAccess: value.bathroomAccess || value.bathroom_access || value.bathroom?.access, bathroomConfidence: value.bathroomConfidence ?? value.bathroom_confidence ?? value.bathroom?.confidence, bathroomEvidenceCount: value.bathroomEvidenceCount ?? value.bathroom_evidence_count ?? value.bathroom?.evidenceCount, bathroomSignal: value.bathroomSignal || value.bathroom_signal, validation: value.validation || 'pending' }; };
const hasCoordinates = stop => Number.isFinite(Number(stop?.latitude)) && Number.isFinite(Number(stop?.longitude));
const stopIsValid = stop => Boolean(stop?.locationId) && stop.validation !== 'stale' && hasCoordinates(stop);
const bathroomPresentation = stop => { const status = String(stop?.bathroomStatus || '').toLowerCase(); const confidence = Number(stop?.bathroomConfidence); const evidence = Number(stop?.bathroomEvidenceCount); let label = stop?.bathroomSignal || ''; if (!label) { if (status === 'confirmed') label = '🚻 Confirmed'; else if (status === 'likely') label = Number.isFinite(confidence) && confidence > 0 ? `🚻 Likely · ${Math.round(confidence <= 1 ? confidence * 100 : confidence)}%` : '🚻 Likely'; else if (status === 'uncertain') label = '🚻 Uncertain'; else if (status === 'no_bathroom') label = '🚻 No bathroom signal'; } const access = stop?.bathroomAccess ? String(stop.bathroomAccess).replace(/_/g, ' ') : ''; return { status, label, access, evidence: Number.isFinite(evidence) && evidence > 0 ? evidence : 0 }; };

export default function RouteSurface() {
  const { services, configured, user } = useAppContext();
  const [params] = useSearchParams();
  const initialLocationId = params.get('locationId') || '';
  const [origin, setOrigin] = useState(params.get('origin') || '');
  const [destination, setDestination] = useState(params.get('destination') || '');
  const [locationId, setLocationId] = useState('');
  const [stops, setStops] = useState(() => { const d = readDraft(); const raw = Array.isArray(d?.stops) ? d.stops : []; const ids = raw.map((s, i) => asSnapshot(s, d?.stopLocationIds?.[i])).filter(Boolean); const incoming = initialLocationId && !ids.some(s => s.locationId === initialLocationId) ? [...ids, { locationId: initialLocationId, validation: 'pending' }] : ids; return incoming; });
  const [route, setRoute] = useState(null);
  const [status, setStatus] = useState(initialLocationId ? 'Restroom stop selected. Add more stops or build your route.' : 'Start with where you are going, or choose restroom stops from Explore.');
  const [busy, setBusy] = useState('');

  useEffect(() => {
    const draft = readDraft();
    const raw = Array.isArray(draft?.stops) ? draft.stops : [];
    let nextStops = raw.map((s, i) => asSnapshot(s, draft?.stopLocationIds?.[i])).filter(s => s?.locationId);
    if (initialLocationId && !nextStops.some(s => s.locationId === initialLocationId)) nextStops.push({ locationId: initialLocationId, validation: 'pending' });
    if (nextStops.length) setStops(nextStops);
    if (draft?.origin && !origin) setOrigin(draft.origin);
    if (draft?.destination && !destination) setDestination(draft.destination);
  }, [initialLocationId]);
  useEffect(() => { writeDraft({ origin, destination, stops, stopLocationIds: stops.map(s => s.locationId) }); window.dispatchEvent(new CustomEvent('kleenest:route-draft-updated', { detail: { stops } })); }, [origin, destination, stops]);
  useEffect(() => {
    const unresolved = stops.filter(s => s?.locationId && (!s.name || s.name === 'Restroom stop' || s.validation === 'pending'));
    if (!unresolved.length || !services?.locations?.getById) return undefined;
    let active = true;
    Promise.all(unresolved.map(async stop => {
      try {
        const place = await services.locations.getById(stop.locationId);
        return { id: stop.locationId, place, error: null };
      } catch (error) {
        return { id: stop.locationId, place: null, error };
      }
    })).then(results => {
      if (!active) return;
      setStops(current => current.map(stop => {
        const result = results.find(r => r.id === stop.locationId);
        if (!result) return stop;
        const hit = result.place;
        if (!hit) return { ...stop, validation: 'stale', validationMessage: 'This location is no longer available.' };
        const latitude = hit.latitude ?? hit.lat ?? stop.latitude;
        const longitude = hit.longitude ?? hit.lng ?? stop.longitude;
        return { ...stop, name: hit.name || stop.name, address: [hit.address, hit.city, hit.state].filter(Boolean).join(', ') || stop.address, latitude, longitude, bathroomStatus: hit.bathroom_status ?? hit.bathroomStatus ?? stop.bathroomStatus, bathroomAccess: hit.bathroom_access ?? hit.bathroomAccess ?? stop.bathroomAccess, bathroomConfidence: hit.bathroom_confidence ?? hit.bathroomConfidence ?? stop.bathroomConfidence, bathroomEvidenceCount: hit.bathroom_evidence_count ?? hit.bathroomEvidenceCount ?? stop.bathroomEvidenceCount, bathroomSignal: hit.bathroom_signal ?? hit.bathroomSignal ?? stop.bathroomSignal, validation: hasCoordinates({ latitude, longitude }) ? 'valid' : 'stale', validationMessage: hasCoordinates({ latitude, longitude }) ? '' : 'This location has no usable coordinates.' };
      }));
    });
    return () => { active = false; };
  }, [services, stops]);
  useEffect(() => { if (origin || !navigator.geolocation) return undefined; let active = true; navigator.geolocation.getCurrentPosition(({ coords }) => active && setOrigin(`${coords.latitude},${coords.longitude}`), () => {}, { enableHighAccuracy: true, timeout: 10000, maximumAge: 120000 }); return () => { active = false; }; }, [origin]);

  const useGps = () => { if (!navigator.geolocation) return setStatus('GPS is unavailable on this device.'); setBusy('gps'); navigator.geolocation.getCurrentPosition(({ coords }) => { setOrigin(`${coords.latitude},${coords.longitude}`); setStatus('Starting point updated.'); setBusy(''); }, () => { setStatus('Location permission was unavailable. Enter an address instead.'); setBusy(''); }, { enableHighAccuracy: true, timeout: 12000, maximumAge: 120000 }); };
  const addStop = async () => { const value = locationId.trim(); if (!value) return setStatus('Enter or select a location before adding a stop.'); if (stops.some(s => s.locationId === value)) return setStatus('That restroom is already in your route.'); let snapshot = { locationId: value, name: 'Restroom stop', validation: 'pending' }; if (services?.locations?.getById) { try { const place = await services.locations.getById(value); if (place) { const latitude = place.latitude ?? place.lat; const longitude = place.longitude ?? place.lng; snapshot = { ...snapshot, name: place.name || 'Restroom stop', address: [place.address, place.city, place.state].filter(Boolean).join(', '), latitude, longitude, bathroomStatus: place.bathroom_status ?? place.bathroomStatus, bathroomAccess: place.bathroom_access ?? place.bathroomAccess, bathroomConfidence: place.bathroom_confidence ?? place.bathroomConfidence, bathroomEvidenceCount: place.bathroom_evidence_count ?? place.bathroomEvidenceCount, bathroomSignal: place.bathroom_signal ?? place.bathroomSignal, validation: hasCoordinates({ latitude, longitude }) ? 'valid' : 'stale', validationMessage: hasCoordinates({ latitude, longitude }) ? '' : 'This location has no usable coordinates.' }; } else snapshot = { ...snapshot, validation: 'stale', validationMessage: 'This location is no longer available.' }; } catch (_) { snapshot = { ...snapshot, validation: 'stale', validationMessage: 'This location could not be verified.' }; } } setStops(v => [...v, snapshot]); setLocationId(''); setRoute(null); setStatus(`Restroom stop ${stops.length + 1} added.`); };
  const removeStop = stop => { const id = typeof stop === 'string' ? stop : stop.locationId; setStops(v => v.filter(x => x.locationId !== id)); setRoute(null); setStatus('Restroom stop removed.'); };
  const clearStops = () => { setStops([]); setRoute(null); setStatus('Restroom stops cleared.'); };
  const moveStop = (index, direction) => { setStops(current => { const next = [...current], target = index + direction; if (target < 0 || target >= next.length) return current; [next[index], next[target]] = [next[target], next[index]]; return next; }); setRoute(null); setStatus('Restroom stop order updated.'); };
  const request = async () => { const invalid = stops.filter(s => !stopIsValid(s)); if (invalid.length) return setStatus(`${invalid.length} restroom stop${invalid.length === 1 ? '' : 's'} need${invalid.length === 1 ? 's' : ''} attention before the route can be built. Remove or replace the unavailable stop${invalid.length === 1 ? '' : 's'}.`); if (!origin.trim()) return setStatus('Enter a starting point before building the route.'); if (!destination.trim() && !stops.length) return setStatus('Add a destination or at least one restroom stop.'); setBusy('route'); try { const selectedStopIds = stops.map(s => s.locationId).filter(Boolean); setStatus(selectedStopIds.length ? `Building the route through ${selectedStopIds.length} restroom stop${selectedStopIds.length === 1 ? '' : 's'}…` : 'Building your route…'); const data = await services.routing.request({ origin: origin.trim(), destination: destination.trim(), locationId: selectedStopIds[selectedStopIds.length - 1] || null, stopLocationIds: selectedStopIds }); setRoute({ ...data, stopLocationIds: data?.stopLocationIds || selectedStopIds }); setStatus('Route ready.'); window.dispatchEvent(new CustomEvent('kleenest:route-updated', { detail: { locationId: data?.locationId || selectedStopIds[selectedStopIds.length - 1] || null, route: data, stopLocationIds: selectedStopIds } })); } catch (e) { setRoute(null); setStatus(e?.message || 'We could not build that route.'); } finally { setBusy(''); } };
  const lifecycle = async (action, label) => { if (!user) return setStatus('Sign in to share route progress.'); setBusy(action); try { await services.routing[action]({ locationId: route?.locationId || stops[stops.length - 1]?.locationId || null, route: route ? { ...route, stopLocationIds: stops.map(s => s.locationId) } : route }); setStatus(label); window.dispatchEvent(new CustomEvent('kleenest:route-updated', { detail: { action, route, stopLocationIds: stops.map(s => s.locationId) } })); } catch (e) { setStatus(e?.message || `Unable to ${action} route.`); } finally { setBusy(''); } };
  const openExternalNavigation = () => { const target = destination || (route?.destination && route.destination !== 'Selected destination' ? route.destination : ''); if (!target) return setStatus('Build a route to a named destination before opening navigation.'); window.open(`https://www.google.com/maps/dir/?api=1&origin=${encodeURIComponent(origin)}&destination=${encodeURIComponent(target)}`, '_blank', 'noopener,noreferrer'); };

  const invalidStopCount = stops.filter(s => !stopIsValid(s)).length;
  return <WorkspaceShell workspace="consumer"><main className="page route-page">
    <header className="route-hero page-header"><div className="route-hero-copy"><span className="eyebrow">SMART ROUTES</span><h1>Plan the trip, not just the destination.</h1><p>Build a route around where you are going and the restroom stops that make the trip easier.</p></div><button className="button secondary route-gps" onClick={useGps} disabled={busy !== ''}><LocateFixed size={17} />Use my location</button></header>
    <section className="detail-panel route-planner"><div className="panel-heading route-panel-heading"><div><span className="eyebrow">PLAN</span><h2>Where are you starting and going?</h2></div><div className="route-heading-icon"><Navigation size={20} /></div></div>
      <div className="route-fields"><label>Starting point<input value={origin} onChange={e => setOrigin(e.target.value)} placeholder="Current location or address" /></label><label>Destination <span className="muted">optional when restroom stops are selected</span><input value={destination} onChange={e => setDestination(e.target.value)} placeholder="Where are you going?" /></label></div>
      <div className="route-stop-picker"><div className="route-stop-picker-heading"><div><span className="route-field-title">Restroom stops · {stops.length}</span><span className="route-field-help">Add as many stops as you need. They are visited in the order shown below.</span></div><div className="route-actions"><Link className="button secondary route-explore" to="/map"><MapPin size={15} />Add from map</Link>{stops.length > 0 && <button className="button secondary" onClick={clearStops}><Trash2 size={15} />Clear stops</button>}</div></div>
        <div className="manual-stop-row"><input value={locationId} onChange={e => setLocationId(e.target.value)} onKeyDown={e => { if (e.key === 'Enter') addStop(); }} placeholder="Paste a location ID" aria-label="Location ID" /><button className="button secondary" onClick={addStop} disabled={!locationId.trim() || busy !== ''}><Plus size={15} />Add stop</button></div>
        {stops.length === 0 ? <div className="stop-empty"><MapPin size={18} /><span>No restroom stops selected yet.</span><Link to="/map">Choose stops on the map</Link></div> : <div className="route-stop-list">{stops.map((s, i) => { const info = bathroomPresentation(s); const invalid = !stopIsValid(s); return <div className={`route-stop ${invalid ? 'route-stop-invalid' : ''}`} key={s.locationId}><GripVertical size={15} aria-hidden="true" /><span className="route-stop-number">{i + 1}</span><div className="route-stop-main"><strong>{s.name || 'Restroom stop'}</strong>{s.address && <small>{s.address}</small>}<div className="route-stop-intelligence">{invalid ? <span className="route-bathroom-signal stale"><AlertTriangle size={13} />{s.validationMessage || 'Location unavailable for navigation'}</span> : info.label && <span className={`route-bathroom-signal ${info.status || 'unknown'}`}>{info.label}</span>}{!invalid && info.access && <span className="route-bathroom-access">{info.access}</span>}{!invalid && info.evidence > 0 && <span className="route-bathroom-evidence">{info.evidence} evidence</span>}</div></div><div className="route-stop-order"><button className="icon-button" onClick={() => moveStop(i, -1)} disabled={i === 0 || busy !== ''} aria-label="Move stop earlier">↑</button><button className="icon-button" onClick={() => moveStop(i, 1)} disabled={i === stops.length - 1 || busy !== ''} aria-label="Move stop later">↓</button><button className="icon-button" onClick={() => removeStop(s)} disabled={busy !== ''} aria-label="Remove stop"><Trash2 size={15} /></button></div></div>; })}</div>}
      </div><div className="route-actions"><button className="button primary route-build" disabled={!configured || busy !== '' || !origin.trim() || (!destination.trim() && !stops.length) || invalidStopCount > 0} onClick={request}><Navigation size={16} />{busy === 'route' ? 'Building route…' : invalidStopCount > 0 ? `Fix ${invalidStopCount} stop${invalidStopCount === 1 ? '' : 's'} first` : 'Build my route'}</button>{!stops.length && <Link className="button secondary" to="/map"><MapPin size={15} />Explore stops</Link>}</div><div className="route-status" role="status"><Sparkles size={16} />{status}</div>
    </section>
    {route && <><section className="detail-panel route-result"><div className="panel-heading"><div><span className="eyebrow">ROUTE READY</span><h2>{route.origin || origin} → {destinationLabel(route, destination, stops[stops.length - 1]?.name)}</h2></div><RouteIcon size={22} /></div><div className="route-metrics"><div className="metric-card"><strong>{formatDistance(route)}</strong><span>distance</span></div><div className="metric-card"><strong>{formatDuration(route)}</strong><span>estimated time</span></div><div className="metric-card"><strong>{route.stopLocationIds?.length ?? stops.length}</strong><span>restroom stops</span></div></div><div className="route-actions route-result-actions"><button className="button primary" onClick={() => lifecycle('start', 'Route started.')} disabled={busy !== ''}><Play size={15} />Start route</button><button className="button secondary" onClick={() => lifecycle('approaching', 'You are approaching your destination.')} disabled={busy !== ''}><MapPin size={15} />Approaching</button><button className="button secondary" onClick={() => lifecycle('arrived', 'Arrival recorded.')} disabled={busy !== ''}><CheckCircle2 size={15} />Arrived</button><button className="button secondary" onClick={openExternalNavigation}><ExternalLink size={15} />Open navigation</button><Link className="button secondary" to="/play/quest"><Trophy size={15} />Trust Quests</Link></div></section>{Array.isArray(route.steps) && route.steps.length > 0 && <section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">YOUR TRIP</span><h2>Turn-by-turn</h2></div><Clock3 size={21} /></div><ol className="route-steps">{route.steps.slice(0, 30).map((s, i) => <li key={`${i}-${s.instruction}`}><strong>{i + 1}</strong><div><span>{s.instruction}</span><small>{s.distanceMeters != null ? `${Math.round(s.distanceMeters)} m` : ''}{s.durationSeconds != null ? ` · ${Math.max(1, Math.round(s.durationSeconds / 60))} min` : ''}</small></div></li>)}</ol></section>}</>}
  </main></WorkspaceShell>;
}
