import { useEffect,useState } from 'react';import { Heart,MessageCircle,RefreshCw,Trophy,UserRound,Users } from 'lucide-react';import { Link } from 'react-router-dom';import { useAppContext } from '../AppContext.jsx';import WorkspaceShell from './WorkspaceShell.jsx';
export default function SocialPage(){const{services,user}=useAppContext();const[reviews,setReviews]=useState([]),[leaders,setLeaders]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState('');const load=async()=>{setLoading(true);setError('');try{const[feed,board]=await Promise.all([services.community.listRecentReviews(12),services.progression.leaderboard(10)]);setReviews(Array.isArray(feed)?feed:[]);setLeaders(Array.isArray(board)?board:[])}catch(e){setError(e.message||'Community activity is temporarily unavailable.')}finally{setLoading(false)}};useEffect(()=>{if(user)void load();else setLoading(false)},[user]);return <WorkspaceShell><section className="page"><div className="page-header"><div><span className="eyebrow">KLEENEST COMMUNITY</span><h1>Social</h1><p>See what the community is discovering, verifying, and contributing.</p></div><button className="secondary" onClick={load} disabled={loading}><RefreshCw size={16}/>Refresh</button></div>{error&&<p className="form-error" role="alert">{error}</p>}{loading?<div className="empty-state">Loading community activity…</div>:<div className="social-grid"><section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">COMMUNITY FEED</span><h2>Recent reviews</h2></div><Users size={24}/></div>{reviews.length?reviews.map(r=><article className="social-post" key={r.id}><div className="social-avatar"><UserRound size={18}/></div><div><strong>{r.profiles?.display_name||'Kleenest member'}</strong><span className="social-time">{r.created_at?new Date(r.created_at).toLocaleString():''}</span><p><b>{r.places?.name||'A Kleenest location'}</b> — {r.rating??r.stars}/5</p><p>{r.body??r.comment}</p><div className="social-actions"><span><Heart size={14}/> Community signal</span><span><MessageCircle size={14}/> Review</span></div></div></article>):<p>No community reviews yet.</p>}</section><aside className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">COMMUNITY RANKINGS</span><h2>Top contributors</h2></div><Trophy size={24}/></div>{leaders.length?leaders.map((r,i)=><div className="reward-row" key={r.user_id||r.id||i}><div><strong>#{i+1} {r.display_name||r.name||'Kleenest member'}</strong><span>{r.level!=null?`Level ${r.level} · `:''}{r.total_check_ins??r.check_ins??0} check-ins</span></div><b>{r.points??r.total_points??0}</b></div>):<p>No leaderboard data yet.</p>}<div className="hero-actions"><Link className="primary" to="/play">Earn rewards</Link><Link className="secondary" to="/leaderboards">Full leaderboard</Link></div></aside></div>}<section className="detail-panel social-callout"><span className="eyebrow">THE LOOP</span><h2>Discover → Check in → Contribute → Earn → Compete</h2><p>Your activity strengthens location intelligence while your contributions build reputation and rewards.</p><div className="hero-actions"><Link className="secondary" to="/map">Explore the map</Link><Link className="secondary" to="/play">Play & rewards</Link></div></section></section></WorkspaceShell>}
