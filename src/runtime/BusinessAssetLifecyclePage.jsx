import{useEffect,useState}from'react';import{CalendarDays,Gift,Megaphone,Power,RefreshCw,Trophy,QrCode}from'lucide-react';import{Link}from'react-router-dom';import{useAppContext}from'../AppContext.jsx';import WorkspaceShell from'./WorkspaceShell.jsx';import CapabilityGate from'./CapabilityGate.jsx';const arr=v=>Array.isArray(v)?v:[];const Card=({icon:Icon,title,children})=><section className="detail-panel business-card"><div className="panel-heading"><div><span className="eyebrow">ASSET LIFECYCLE</span><h2>{title}</h2></div><Icon size={20}/></div>{children}</section>;const Action=({busy,children,...p})=><button className="secondary" disabled={busy} {...p}>{busy?'Saving…':children}</button>;export default function BusinessAssetLifecyclePage(){const{services,user,isPlatformOwner,businessMemberships,selectedBusinessId,setSelectedBusinessId}=useAppContext();const[business,setBusiness]=useState(null),[locations,setLocations]=useState([]),[qrs,setQrs]=useState([]),[campaigns,setCampaigns]=useState([]),[promotions,setPromotions]=useState([]),[events,setEvents]=useState([]),[contests,setContests]=useState([]),[loading,setLoading]=useState(true),[busy,setBusy]=useState(''),[message,setMessage]=useState(''),[error,setError]=useState('');const businesses=arr(businessMemberships);const load=async()=>{setLoading(true);setError('');try{const bs=businesses.length?businesses:arr(await services.business.listBusinesses({includeDemo:isPlatformOwner}));const b=bs.find(x=>String(x.business_id||x.id)===String(selectedBusinessId))||bs[0]||null;setBusiness(b);if(!b){setLocations([]);setQrs([]);setCampaigns([]);setPromotions([]);setEvents([]);setContests([]);return}const id=b.business_id||b.id;if(String(id)!==String(selectedBusinessId))setSelectedBusinessId(id);const[l,q,c,p,e,x]=await Promise.all([services.business.listLocations(id),services.business.listQrs(id),services.business.listCampaigns(id),services.business.listPromotions(id),services.business.listEvents(id),services.business.listContests(id)]);setLocations(arr(l));setQrs(arr(q));setCampaigns(arr(c));setPromotions(arr(p));setEvents(arr(e));setContests(arr(x))}catch(e){setError(e.message||'Unable to load asset lifecycle.')}finally{setLoading(false)}};useEffect(()=>{if(user)void load();else setLoading(false)},[user,isPlatformOwner,selectedBusinessId,businesses.length]);useEffect(()=>{const refresh=()=>void load();window.addEventListener('kleenest:business-updated',refresh);window.addEventListener('kleenest:asset-updated',refresh);return()=>{window.removeEventListener('kleenest:business-updated',refresh);window.removeEventListener('kleenest:asset-updated',refresh)}},[user,selectedBusinessId]);const run=async(key,fn,actionName)=>{setBusy(key);setMessage('');setError('');try{await fn();try{await services.analytics.businessAction(business?.business_id||business?.id,actionName,{businessId:business?.business_id||business?.id,metadata:{asset_action:actionName}})}catch{}setMessage('Asset updated.');window.dispatchEvent(new CustomEvent('kleenest:asset-updated'));window.dispatchEvent(new CustomEvent('kleenest:business-updated'))}catch(e){setError(e.message||'Unable to update asset.')}finally{setBusy('')}};if(!user)return <WorkspaceShell workspace="business"><section className="empty-state"><h2>Sign in to manage assets</h2><Link className="primary" to="/auth">Sign in</Link></section></WorkspaceShell>;if(loading)return <WorkspaceShell workspace="business"><section className="empty-state">Loading asset lifecycle…</section></WorkspaceShell>;if(!business)return <WorkspaceShell workspace="business"><section className="empty-state"><h2>No business account</h2><Link className="secondary" to="/business">Business dashboard</Link></section></WorkspaceShell>;const id=business.business_id||business.id;return <WorkspaceShell workspace="business"><section className="page"><div className="page-header"><div><span className="eyebrow">BUSINESS OPERATIONS</span><h1>Asset lifecycle</h1><p>Create, activate, pause, update, and retire the assets customers interact with.</p></div><div className="hero-actions">{businesses.length>1&&<label className="form-field compact-field"><span>Business</span><select value={String(selectedBusinessId||id)} onChange={e=>setSelectedBusinessId(e.target.value)}>{businesses.map(b=><option key={b.business_id||b.id} value={b.business_id||b.id}>{b.name||b.business_name||'Business'}</option>)}</select></label>}<Link className="secondary" to="/business/manage">Create assets</Link><Link className="secondary" to="/business/engagement">Engagement</Link><button className="secondary" onClick={load}><RefreshCw size={16}/>Refresh</button></div></div>{message&&<p className="form-success" role="status">{message}</p>}{error&&<p className="form-error" role="alert">{error}</p>}<CapabilityGate kind="business" businessId={id} fallback={<div className="empty-state"><h2>Asset lifecycle unavailable</h2><p>Your current business capability does not permit asset lifecycle operations.</p><Link className="secondary" to="/business">Business dashboard</Link></div>}><div className="business-manage-grid"><Card icon={Power} title="Locations">{locations.slice(0,10).map(x=><div className="management-item" key={x.id}><span><strong>{x.name||'Location'}</strong><small>{x.is_active===false?'Inactive':'Active'}</small></span><Action busy={busy===`location-${x.id}`} onClick={()=>run(`location-${x.id}`,()=>services.business.setLocationActive(id,x.id,x.is_active===false),'location_active_toggle')}><Power size={15}/>{x.is_active===false?'Activate':'Deactivate'}</Action></div>)}{!locations.length&&<p>No locations yet.</p>}</Card><Card icon={QrCode} title="QR programs">{qrs.slice(0,10).map(x=><div className="management-item" key={x.id}><span><strong>{x.label||'QR code'}</strong><small>{x.active===false?'Inactive':'Active'}</small></span><Action busy={busy===`qr-${x.id}`} onClick={()=>run(`qr-${x.id}`,()=>services.business.setQrActive(id,x.id,x.active===false),'qr_active_toggle')}><Power size={15}/>{x.active===false?'Activate':'Deactivate'}</Action></div>)}{!qrs.length&&<p>No QR assets yet.</p>}</Card><Card icon={Megaphone} title="Campaigns">{campaigns.slice(0,10).map(x=><div className="management-item" key={x.id}><span><strong>{x.name||x.title||'Campaign'}</strong><small>{x.status||'draft'}</small></span><Action busy={busy===`campaign-${x.id}`} onClick={()=>run(`campaign-${x.id}`,()=>services.business.pauseCampaign(id,x.id),'campaign_pause')} >{String(x.status).toLowerCase()==='paused'?'Paused':'Pause'}</Action></div>)}{!campaigns.length&&<p>No campaigns yet.</p>}</Card><Card icon={Gift} title="Promotions">{promotions.slice(0,10).map(x=><div className="management-item" key={x.id}><span><strong>{x.title||x.name||'Promotion'}</strong><small>{x.active===false?'Inactive':'Active'}</small></span><Action busy={busy===`promotion-${x.id}`} onClick={()=>run(`promotion-${x.id}`,()=>services.business.managePromotion(id,x.id,x.active===false?'activate':'deactivate',{}),'promotion_status_toggle')}><Power size={15}/>{x.active===false?'Activate':'Deactivate'}</Action></div>)}{!promotions.length&&<p>No promotions yet.</p>}</Card><Card icon={CalendarDays} title="Events">{events.slice(0,10).map(x=><div className="management-item" key={x.id}><span><strong>{x.title||x.name||'Event'}</strong><small>{x.event_date||'Scheduled'}</small></span><Action busy={busy===`event-${x.id}`} onClick={()=>run(`event-${x.id}`,()=>services.business.deleteEvent(id,x.id),'event_delete')}>Delete</Action></div>)}{!events.length&&<p>No events yet.</p>}</Card><Card icon={Trophy} title="Contests">{contests.slice(0,10).map(x=><div className="management-item" key={x.id}><span><strong>{x.name||x.title||'Contest'}</strong><small>{x.status||'draft'}</small></span><Action busy={busy===`contest-${x.id}`} onClick={()=>run(`contest-${x.id}`,()=>services.business.deleteContest(id,x.id),'contest_delete')}>Delete</Action></div>)}{!contests.length&&<p>No contests yet.</p>}</Card></div></CapabilityGate></section></WorkspaceShell>}