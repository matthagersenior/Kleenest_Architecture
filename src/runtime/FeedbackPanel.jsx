import{useState}from'react';import{MessageSquare}from'lucide-react';import{useAppContext}from'../AppContext.jsx';
export default function FeedbackPanel(){const{services}=useAppContext();const[type,setType]=useState('feedback');const[title,setTitle]=useState('');const[description,setDescription]=useState('');const[busy,setBusy]=useState(false);const[message,setMessage]=useState('');const[error,setError]=useState('');const submit=async e=>{e.preventDefault();setBusy(true);setError('');setMessage('');try{await services.feedback.submit({type,title,description,page:window.location.pathname,metadata:{source:'feedback_panel'}});setTitle('');setDescription('');setMessage('Feedback submitted.')}catch(err){setError(err.message||'Unable to submit feedback.')}finally{setBusy(false)}};return <form className="detail-panel" onSubmit={submit}><div className="panel-heading"><div><span className="eyebrow">PRODUCT FEEDBACK</span><h2>Tell us what needs attention</h2></div><MessageSquare size={20}/></div>{error&&<p className="form-error" role="alert">{error}</p>}{message&&<p className="form-success" role="status">{message}</p>}<label className="form-field"><span>Type</span><select value={type} onChange={e=>setType(e.target.value)}><option value="feedback">Feedback</option><option value="bug">Bug</option><option value="feature">Feature request</option><option value="safety">Safety</option></select></label><label className="form-field"><span>Title</span><input required value={title} onChange={e=>setTitle(e.target.value)}/></label><label className="form-field"><span>Description</span><textarea required value={description} onChange={e=>setDescription(e.target.value)}/></label><button className="primary" disabled={busy} type="submit"><MessageSquare size={16}/>{busy?'Submitting…':'Submit feedback'}</button></form>}
