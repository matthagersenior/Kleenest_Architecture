import{useEffect,useState}from'react';import{RefreshCw,Database,ArrowRight,ShieldCheck,AlertTriangle,CheckCircle2}from'lucide-react';import{Link}from'react-router-dom';import{useAppContext}from'../AppContext.jsx';import WorkspaceShell from'./WorkspaceShell.jsx';
const arr=v=>Array.isArray(v)?v:(Array.isArray(v?.rows)?v.rows:Array.isArray(v?.data)?v.data:[]);const statusOf=v=>String(v?.status||v?.issue||v?.result||'').toLowerCase();const isBad=v=>['error','failed','missing','duplicate','blocked','not_ok'].some(x=>statusOf(v).includes(x));
export default function OwnerAuditPage(){const{profile,services}=useAppContext();const[events,setEvents]=useState([]),[rawAudit,setRawAudit]=useState([]),[contracts,setContracts]=useState([]);const[loading,setLoading]=useState(true),[auditLoading,setAuditLoading]=useState(false),[error,setError]=useState('');const load=async()=>{setLoading(true);setError('');try{const v=await services.admin.invoke(profile,'admin_crud_gateway',{p_resource:'activity_events',p_action:'list',p_id:null,p_payload:{limit:100,order:'desc'}});setEvents(arr(v))}catch(e){setError(e.message||'Unable to load audit activity.')}finally{setLoading(false)}};const runArchitectureAudit=async()=>{setAuditLoading(true);setError('');try{const[raw,contract]=await Promise.all([services.admin.invoke(profile,'admin_raw_schema_capability_audit',{}),services.admin.invoke(profile,'check_single_capability_per_domain',{})]);setRawAudit(arr(raw));setContracts(arr(contract))}catch(e){setError(e.message||'Unable to run the raw-schema capability audit.')}finally{setAuditLoading(false)}};useEffect(()=>{void load();void runArchitectureAudit()},[profile,services]);const badRaw=rawAudit.filter(isBad).length,badContracts=contracts.filter(isBad).length;return <WorkspaceShell workspace="owner"><section className="page"><div className="page-header"><div><span className="eyebrow">PLATFORM</span><h1>Audit & activity</h1><p>Owner-authorized operational activity plus a raw-schema capability audit. The database is the authority; documentation is evidence, not the source of truth.</p></div><div className="hero-actions"><button className="button secondary" onClick={()=>{void load();void runArchitectureAudit()}} disabled={loading||auditLoading}><RefreshCw size={16}/>Refresh all</button><Link className="button secondary" to="/owner/operational-capabilities"><Database size={16}/>Hidden capabilities <ArrowRight size={15}/></Link></div></div>{error&&<div className="state error" role="alert">{error}</div>}<section className="detail-grid"><article className="metric-card"><strong>{badRaw?'Attention required':'Raw schema clean'}</strong><span>{rawAudit.length} raw-schema audit findings · {badRaw} flagged</span></article><article className="metric-card"><strong>{badContracts?'Contract drift':'Domain contracts healthy'}</strong><span>{contracts.length} domains checked · {badContracts} flagged</span></article><article className="metric-card"><strong>Owner CRUD</strong><span>Use the governed Owner CRUD workbench for operational fixes.</span></article></section><section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">RAW SCHEMA → CAPABILITY INVENTORY</span><h2>Undocumented capabilities cannot hide</h2></div><Database size={22}/></div>{auditLoading&&!rawAudit.length?<p>Scanning PostgreSQL schema…</p>:!rawAudit.length?<p className="muted">No audit rows were returned.</p>:<div className="stack-list">{rawAudit.slice(0,100).map((e,i)=><article className="list-row" key={`${e.domain||e.schema_name||e.function_name||'audit'}-${i}`}><div><strong>{e.capability||e.canonical_capability||e.function_name||e.table_name||e.domain||'Schema capability'}</strong><small>{e.domain||e.schema_name||e.object_type||'public'}{e.canonical_rpc?` · ${e.canonical_rpc}`:''}</small></div><span>{isBad(e)?<AlertTriangle size={17}/>:<CheckCircle2 size={17}/>} {e.issue||e.status||e.result||'ok'}</span></article>)}</div>}</section><section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">SINGLE CAPABILITY PER DOMAIN</span><h2>Canonical boundary enforcement</h2></div><ShieldCheck size={22}/></div>{!contracts.length?<p className="muted">Run the audit to verify canonical domain contracts.</p>:<div className="stack-list">{contracts.map((c,i)=><article className="list-row" key={`${c.domain||'domain'}-${i}`}><div><strong>{c.domain||c.feature_code||'Domain'}</strong><small>{c.canonical_capability||c.capability||'Canonical capability'}{c.canonical_rpc?` · ${c.canonical_rpc}`:''}</small></div><span>{isBad(c)?<AlertTriangle size={17}/>:<CheckCircle2 size={17}/>} {c.issue||c.status||'ok'}</span></article>)}</div>}</section><section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">ACTIVITY</span><h2>Recent operational events</h2></div></div>{loading?<p>Loading activity…</p>:!events.length?<p className="muted">No activity events were returned.</p>:<div className="stack-list">{events.map((e,i)=><article className="list-row" key={e.id||e.event_id||i}><div><strong>{e.event_type||e.action||e.type||'Activity event'}</strong><small>{e.created_at||e.timestamp||e.occurred_at||'Time unavailable'}{e.actor_id?` · ${e.actor_id}`:''}</small></div><span>{e.status||e.result||e.resource||''}</span></article>)}</div>}</section></section></WorkspaceShell>}
