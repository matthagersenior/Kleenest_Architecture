import{useEffect,useState}from'react';import{CheckCircle2,RefreshCw,Send,ShieldCheck,Camera,HeartPulse,Sparkles,ArrowRight,Trophy}from'lucide-react';import{useSearchParams,Link}from'react-router-dom';import{useAppContext}from'../AppContext.jsx';import WorkspaceShell from'./WorkspaceShell.jsx';
function Result({body}){if(!body)return null;const entries=Array.isArray(body)?body:(body?.rows||body?.data||[body]);return <div className="state success evidence-result" role="status"><CheckCircle2 size={19}/><div><strong>Evidence added to Kleenest.</strong><div className="detail-grid">{entries.slice(0,6).map((item,i)=><div className="metric-card" key={i}><strong>{typeof item==='object'?item.name||item.title||item.status||item.message||`Signal ${i+1}`:String(item)}</strong></div>)}</div></div></div>}
const signals=[['cleanliness','Cleanliness','How clean does it feel right now?'],['accessibility','Accessibility','What should visitors with accessibility needs know?'],['safety','Safety','Anything that affects comfort or safety?'],['availability','Availability','Is it open and usable?'],['condition','Condition','What is the overall physical condition?']];
export default function LocationEvidencePage(){const{services}=useAppContext();const[params]=useSearchParams();const locationId=params.get('locationId')||'';const[form,setForm]=useState({locationId,observationType:'condition',cleanlinessPct:'',note:'',stars:5,cleanliness:'',accessibility:'',safety:'',availability:'',condition:'',feedback:'',checkInId:'',photoId:''});const[status,setStatus]=useState(null),[error,setError]=useState(''),[busy,setBusy]=useState('');useEffect(()=>setForm(v=>({...v,locationId})),[locationId]);const set=(k,v)=>setForm(x=>({...x,[k]:v}));async function settleEvidence(result,type,rating=null){await Promise.allSettled([services.analytics.record('location_evidence_submitted',{featureCode:'location_evidence',subjectType:'location',subjectId:form.locationId,locationId:form.locationId,valueNumeric:rating,metadata:{evidence_type:type,check_in_id:form.checkInId||null}}),services.progression.evaluateBadges(),services.progression.refreshMilestones()]);window.dispatchEvent(new CustomEvent('kleenest:location-activity',{detail:{locationId:form.locationId,type}}));window.dispatchEvent(new CustomEvent('kleenest:progression-updated',{detail:{locationId:form.locationId,type}}));setStatus(result)}async function submitObservation(e){e.preventDefault();setBusy('observation');setError('');setStatus(null);try{const result=await services.locationEvidence.restroomObservation({locationId:form.locationId,checkInId:form.checkInId||null,observationType:form.observationType,cleanlinessPct:form.cleanlinessPct||null,note:form.note||null});await settleEvidence(result,'observation')}catch(e){setError(e?.message||String(e))}finally{setBusy('')}}async function submitQuality(e){e.preventDefault();setBusy('quality');setError('');setStatus(null);try{const rating=Number(form.stars);const result=await services.locationEvidence.qualityObservation({locationId:form.locationId,stars:rating,cleanliness:form.cleanliness||null,accessibility:form.accessibility||null,safety:form.safety||null,availability:form.availability||null,condition:form.condition||null,feedback:form.feedback||null,checkInId:form.checkInId||null,photoId:form.photoId||null});await settleEvidence(result,'quality',rating)}catch(e){setError(e?.message||String(e))}finally{setBusy('')}}return <WorkspaceShell workspace="consumer"><main className="page evidence-page"><section className="hero journey-hero"><div><span className="eyebrow">TRUST EVIDENCE</span><h1>Show the next visitor what is really there.</h1><p>Evidence turns a moment at a restroom into better, fresher location intelligence for everyone.</p><div className="home-trust-strip"><span><ShieldCheck size={15}/>Verified where possible</span><span><Camera size={15}/>Useful observations</span><span><Sparkles size={15}/>Progression enabled</span></div></div><div className="hero-actions"><Link className="secondary" to="/map">Back to Map</Link><Link className="secondary" to="/play/quest"><Trophy size={16}/>Trust Quests</Link></div></section>{error&&<div className="state error" role="alert">{error}</div>}{status&&<Result body={status}/>}<section className="detail-grid evidence-choice"><button className="evidence-mode" type="button" onClick={()=>document.getElementById('observation')?.scrollIntoView({behavior:'smooth'})}><CheckCircle2 size={23}/><strong>Quick observation</strong><span>Report what you see now with a focused signal.</span><ArrowRight size={15}/></button><button className="evidence-mode" type="button" onClick={()=>document.getElementById('quality')?.scrollIntoView({behavior:'smooth'})}><HeartPulse size={23}/><strong>Full quality signal</strong><span>Give the location a richer cleanliness and condition assessment.</span><ArrowRight size={15}/></button></section><section className="detail-grid"><form id="observation" className="detail-panel journey-card" onSubmit={submitObservation}><div className="panel-heading"><div><span className="eyebrow">QUICK SIGNAL</span><h2>What did you observe?</h2></div><CheckCircle2 size={22}/></div><label>Location<input value={form.locationId} onChange={e=>set('locationId',e.target.value)} placeholder="Location ID from the place page" required/></label><label>Observation<select value={form.observationType} onChange={e=>set('observationType',e.target.value)}><option value="condition">Condition</option><option value="cleanliness">Cleanliness</option><option value="availability">Availability</option><option value="safety">Safety</option></select></label><label>Cleanliness estimate %<input type="number" min="0" max="100" value={form.cleanlinessPct} onChange={e=>set('cleanlinessPct',e.target.value)} placeholder="Optional"/></label><label>What should people know?<textarea rows="5" value={form.note} onChange={e=>set('note',e.target.value)} placeholder="Keep it factual and useful."/></label><label>Verified visit ID<input value={form.checkInId} onChange={e=>set('checkInId',e.target.value)} placeholder="Optional"/></label><button className="primary" disabled={busy!==''||!form.locationId||(!form.note&&!form.cleanlinessPct)}><Send size={16}/>{busy==='observation'?'Adding signal…':'Add observation'}</button></form><form id="quality" className="detail-panel journey-card" onSubmit={submitQuality}><div className="panel-heading"><div><span className="eyebrow">QUALITY SIGNAL</span><h2>Build the fuller picture</h2></div><Sparkles size={22}/></div><p className="lead">Small details become powerful when enough visitors contribute them.</p><label>Overall experience<select value={form.stars} onChange={e=>set('stars',e.target.value)}><option value="5">★★★★★ · Excellent</option><option value="4">★★★★☆ · Good</option><option value="3">★★★☆☆ · Okay</option><option value="2">★★☆☆☆ · Poor</option><option value="1">★☆☆☆☆ · Bad</option></select></label><div className="signal-grid">{signals.map(([key,label,hint])=><label key={key}><span>{label}</span><small>{hint}</small><input value={form[key]} onChange={e=>set(key,e.target.value)} placeholder="Your signal"/></label>)}</div><label>Anything else?<textarea rows="4" value={form.feedback} onChange={e=>set('feedback',e.target.value)} placeholder="Helpful context, without personal information."/></label><label>Verified visit ID<input value={form.checkInId} onChange={e=>set('checkInId',e.target.value)} placeholder="Optional"/></label><button className="primary" disabled={busy!==''||!form.locationId}>{busy==='quality'?'Publishing signal…':'Publish quality signal'} <ArrowRight size={15}/></button></form></section><section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">AFTER YOU CONTRIBUTE</span><h2>Keep the trust loop moving</h2></div><RefreshCw size={20}/></div><div className="contribution-grid"><Link className="contribution-card" to={`/place/${encodeURIComponent(form.locationId)}`}><ShieldCheck size={20}/><strong>Return to location</strong><span>See how your contribution fits into the location story.</span></Link><Link className="contribution-card" to="/play/quest"><Trophy size={20}/><strong>Continue a Trust Quest</strong><span>Your real-world activity can advance active quests.</span></Link><Link className="contribution-card" to="/activity"><Sparkles size={20}/><strong>See progression</strong><span>Check rewards, XP, and recent contributions.</span></Link></div></section></main></WorkspaceShell>}
