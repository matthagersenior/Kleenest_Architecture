import {Navigate} from 'react-router-dom';import {useAppContext} from '../AppContext.jsx';import {isPlatformOwner} from '../domains/entitlements/access.js';import {getNavigationForWorkspace} from '../domain/workspaces.js';import WorkspaceNavigation from './WorkspaceNavigation.jsx';
export default function WorkspaceShell({children,workspace='consumer'}){const{capabilities=[],loading,profile,membershipTier,presentationTier,workspaceModel}=useAppContext();const owner=isPlatformOwner(profile);const effectiveWorkspace=workspace==='owner'?'admin':workspace;const previewing=owner&&presentationTier!==membershipTier;const allowedByPreview=previewing&&presentationTier===effectiveWorkspace;const allowed=effectiveWorkspace==='admin'?owner:allowedByPreview||getNavigationForWorkspace(effectiveWorkspace,capabilities).length>0;if(loading)return <div className="app-loading" role="status"><div className="loading-mark">K</div><span>Loading Kleenest…</span></div>;if(!allowed)return <Navigate to="/" replace/>;const displayedTier=workspace==='owner'?membershipTier:presentationTier;const availableWorkspaces=workspaceModel?.availableWorkspaces||['consumer'];const handleWorkspaceChange=(nextWorkspace)=>{if(nextWorkspace==='admin'&&!owner)return;const links=getNavigationForWorkspace(nextWorkspace,capabilities);if(links[0]?.path)window.location.hash=links[0].path;};return <div className={`app workspace-${effectiveWorkspace} membership-${displayedTier}`}><WorkspaceNavigation workspace={effectiveWorkspace} capabilities={capabilities} membershipLabel={workspaceModel?.membershipLabel||displayedTier} availableWorkspaces={availableWorkspaces} onWorkspaceChange={handleWorkspaceChange}/>{previewing&&workspace!=='owner'&&<div className="preview-banner">Owner preview · {presentationTier} experience</div>}{owner&&<div className="platform-access"><a href="#/owner">Platform controls</a></div>}<main>{children}</main></div>}