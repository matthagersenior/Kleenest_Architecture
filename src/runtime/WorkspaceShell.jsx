import{Navigate,Link,useNavigate}from'react-router-dom';import{ArrowRight}from'lucide-react';import{useAppContext}from'../AppContext.jsx';import{isPlatformOwner}from'../domains/entitlements/access.js';import{getNavigationForWorkspace,getAvailableWorkspaces,getWorkspace}from'../domain/workspaces.js';import WorkspaceNavigation from'./WorkspaceNavigation.jsx';
const WORKSPACE_ACTIONS={business:{label:'Manage locations & assets',path:'/business/assets'},fleet:{label:'Open Fleet Command',path:'/fleet'},enterprise:{label:'Open Enterprise Command',path:'/enterprise'},admin:{label:'Open Platform CRUD',path:'/owner/data'}};
export default function WorkspaceShell({children,workspace='consumer'}){const{capabilities=[],loading,profile,user,membershipTier,presentationTier,workspaceModel,isPlatformOwner:contextOwner}=useAppContext();const owner=Boolean(contextOwner||isPlatformOwner(profile));const effectiveWorkspace=workspace==='owner'?'admin':workspace;const effectiveCapabilities=owner?Array.from(new Set([...capabilities,'owner','business','fleet','enterprise'])):capabilities;const previewing=owner&&presentationTier!==membershipTier;const allowedByPreview=previewing&&presentationTier===effectiveWorkspace;const allowed=effectiveWorkspace==='admin'?owner:allowedByPreview||getNavigationForWorkspace(effectiveWorkspace,effectiveCapabilities).length>0;const navigate=useNavigate();if(loading)return <div className="app-loading" role="status"><div className="loading-mark">K</div><span>Loading Kleenest…</span></div>;if(!allowed)return <Navigate to="/" replace/>;const displayedTier=workspace==='owner'?'Owner Control':(workspaceModel?.membershipLabel||presentationTier||membershipTier);const availableWorkspaces=owner?getAvailableWorkspaces(effectiveCapabilities):((workspaceModel?.availableWorkspaces?.length)?workspaceModel.availableWorkspaces:['consumer']);const handleWorkspaceChange=(nextWorkspace)=>{if(nextWorkspace==='admin'&&!owner)return;if(nextWorkspace==='consumer'&&!user){navigate('/auth');return}const links=getNavigationForWorkspace(nextWorkspace,effectiveCapabilities);if(nextWorkspace==='admin'){navigate('/admin');return}if(links[0]?.path)navigate(links[0].path)};const meta=getWorkspace(effectiveWorkspace);const action=WORKSPACE_ACTIONS[effectiveWorkspace];return <div className={`app workspace-${effectiveWorkspace} membership-${displayedTier}`}><WorkspaceNavigation workspace={effectiveWorkspace} capabilities={effectiveCapabilities} membershipLabel={displayedTier} availableWorkspaces={availableWorkspaces} onWorkspaceChange={handleWorkspaceChange} isPlatformOwner={owner} authenticated={Boolean(user)}/>{previewing&&workspace!=='owner'&&<div className="preview-banner">Owner preview · {presentationTier} experience</div>}{owner&&<div className="platform-access"><Link to="/owner">Platform controls</Link></div>}{effectiveWorkspace!=='consumer'&&<section className="workspace-command-strip" aria-label={`${meta.label} workspace overview`} style={{maxWidth:1240,margin:'0 auto',padding:'16px 28px 4px',display:'flex',alignItems:'center',justifyContent:'space-between',gap:16}}><div><span className="eyebrow" style={{marginBottom:5}}>{meta.label.toUpperCase()}</span><div style={{color:'#667085',fontSize:13,lineHeight:1.5}}>{meta.description}</div></div>{action&&<Link className="button primary" to={action.path} style={{whiteSpace:'nowrap'}}>{action.label}<ArrowRight size={15}/></Link>}</section>}<main>{children}</main></div>}
