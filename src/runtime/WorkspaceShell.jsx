import {Link,Navigate,useLocation} from 'react-router-dom';import {useAppContext} from '../AppContext.jsx';import {CAPABILITIES,hasCapability,isPlatformOwner} from '../domains/entitlements/access.js';
const nav={consumer:[['Explore','/map'],['Interact','/interaction'],['Routes','/route'],['Activity','/activity'],['Play','/play'],['Community','/community'],['Notifications','/notifications'],['Offline','/offline'],['Family','/family']],business:[['Overview','/business'],['Manage','/business/manage'],['Intelligence','/business/intelligence'],['Engage','/business/engage'],['Analytics','/business/analytics'],['Notifications','/notifications']],fleet:[['Operations','/fleet'],['Routes','/fleet/routes'],['Performance','/fleet/performance'],['Opportunities','/fleet/opportunities'],['Goals','/fleet/goals'],['Intelligence','/fleet/intelligence'],['Notifications','/notifications']],enterprise:[['Command','/enterprise'],['Partners','/enterprise/partners'],['Campaigns','/enterprise/campaigns'],['Performance','/enterprise/performance'],['Fleet','/enterprise/fleet'],['Notifications','/notifications']],owner:[['Control Center','/owner'],['Users','/owner/data'],['Data','/owner/data'],['Tier Preview','/owner/preview'],['Audit','/owner/audit']]};const cap={business:CAPABILITIES.BUSINESS,fleet:CAPABILITIES.FLEET,enterprise:CAPABILITIES.ENTERPRISE};
export default function WorkspaceShell({children,workspace='consumer'}){const l=useLocation(),{capabilities=[],loading,profile,membershipTier,presentationTier}=useAppContext();const owner=isPlatformOwner(profile);const previewing=owner&&presentationTier!==membershipTier;const allowedByPreview=previewing&&presentationTier===workspace;const allowed=(workspace==='owner'&&owner)||allowedByPreview||(cap[workspace]?hasCapability(capabilities,cap[workspace]):true);if(loading)return <div>Loading Kleenest…</div>;if(!allowed)return <Navigate to="/" replace/>;const displayedTier=workspace==='owner'?membershipTier:presentationTier;return <div className={`app workspace-${workspace} membership-${displayedTier}`}><header className="topbar"><Link className="brand" to="/">Kleenest</Link><nav className="nav">{(nav[workspace]||nav.consumer).map(([label,path])=><Link key={`${label}:${path}`} className={l.pathname===path?'active':''} to={path}>{label}</Link>)}</nav><span className="membership-badge">{displayedTier}</span>{owner&&<Link className="owner-link" to="/owner">Platform</Link>}</header>{previewing&&workspace!=='owner'&&<div className="preview-banner">Owner preview · {presentationTier} experience</div>}<main>{children}</main></div>}
