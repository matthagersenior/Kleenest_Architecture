import{Link,useLocation}from'react-router-dom';import{BarChart3,FileText,History,Settings}from'lucide-react';export default function ReportingWorkspaceNav({scope}){const{pathname}=useLocation();const base=`/${scope}/reports`;const items=[['Overview',base,FileText],['History',`${base}/history`,History],['Analytics',scope==='business'?`/${scope}/analytics`:`/${scope}/performance`,BarChart3],['Settings',base,Settings]];return <nav className="workspace-subnav" aria-label="Reporting"><span className="eyebrow">REPORTING</span>{items.map(([label,to,Icon])=><Link key={label} className={pathname===to?'active':''} to={to}><Icon size={16}/>{label}</Link>)}</nav>}