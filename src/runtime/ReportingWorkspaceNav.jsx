import{Link,useLocation}from'react-router-dom';import{BarChart3,FileText,History,Settings}from'lucide-react';
export default function ReportingWorkspaceNav({scope}){const{pathname}=useLocation();const base=`/${scope}/reports`;const items=[['Overview',base,FileText],['History',`${base}/history`,History],['Analytics',scope==='business'?`/${scope}/analytics`:`/${scope}/performance`,BarChart3],['Settings',`${base}/settings`,Settings]];return <nav className="workspace-subnav reporting-subnav" aria-label="Reporting"><div className="workspace-subnav-heading"><span className="eyebrow">REPORTING</span><span className="workspace-subnav-caption">Monitor, automate, review</span></div><div className="workspace-subnav-links">{items.map(([label,to,Icon])=>{const active=pathname===to||(label==='Analytics'&&pathname.startsWith(to+'/'));return <Link key={label} className={`workspace-subnav-link${active?' active':''}`} to={to} aria-current={active?'page':undefined}><Icon size={16}/><span>{label}</span></Link>})}</div></nav>}
