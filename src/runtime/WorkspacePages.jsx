import { useEffect, useState } from 'react';
import { useAppContext } from '../AppContext.jsx';
import WorkspaceShell from './WorkspaceShell.jsx';
import CapabilityPanel, { MetricGrid, ActionForm } from './CapabilityPanel.jsx';
function businessIdFrom(data){const first=Array.isArray(data)?data[0]:data?.businesses?.[0]||data?.data?.[0];return first?.business_id||first?.id||null;}
function Page({workspace,title,children}){return <WorkspaceShell workspace={workspace}><section className="page-heading"><span className="eyebrow">Kleenest</span><h1>{title}</h1></section>{children}</WorkspaceShell>}
function useBusiness(services){const[businesses,setBusinesses]=useState(null);const[refresh,setRefresh]=useState(0);useEffect(()=>{void services.business.listBusinesses().then(setBusinesses).catch(()=>setBusinesses([]))},[services,refresh]);return{id:businessIdFrom(businesses),refresh:()=>setRefresh(v=>v+1)}}
export function ActivityPage(){const{services}=useAppContext();return <Page title="Activity"><CapabilityPanel title="Your activity" load={()=>services.notifications.list()} renderData={data=><MetricGrid items={data}/>}/><CapabilityPanel title="Live activity" load={()=>services.live.list()} renderData={data=><MetricGrid items={data}/>}/><ActionForm title="Mark notification read" submitLabel="Mark read" fields={[{name:'notificationId',label:'Notification ID'}]} onSubmit={v=>services.notifications.markRead(v.notificationId)}/></Page>}
export function PlayPage(){const{services}=useAppContext();return <Page title="Play"><CapabilityPanel title="Progression" load={()=>services.progression.dashboard()} renderData={data=><MetricGrid items={data}/>}/><CapabilityPanel title="Leaderboard" load={()=>services.progression.leaderboard(25)} renderData={data=><MetricGrid items={data}/>}/><CapabilityPanel title="Active contests" load={()=>services.progression.contests(10)} renderData={data=><MetricGrid items={data}/>}/></Page>}
export function CommunityPage(){const{services}=useAppContext();return <Page title="Community"><CapabilityPanel title="Community activity" load={()=>services.live.list()} renderData={data=><MetricGrid items={data}/>}/><CapabilityPanel title="Leaderboard" load={()=>services.progression.leaderboard(25)} renderData={data=><MetricGrid items={data}/>}/></Page>}
export function FamilyPage(){const{services}=useAppContext();return <Page title="Family"><CapabilityPanel title="Premium access" load={()=>services.family.hasPremiumAccess()} renderData={data=><MetricGrid items={data}/>}/><ActionForm title="Create family" submitLabel="Create family" fields={[{name:'name',label:'Family name'}]} onSubmit={v=>services.family.create(v.name)}/><ActionForm title="Invite family member" submitLabel="Send invite" fields={[{name:'familyId',label:'Family ID'},{name:'email',label:'Member email'}]} onSubmit={v=>services.family.invite(v.familyId,v.email)}/></Page>}
export function BusinessPage({section='overview'}){const{services}=useAppContext();const{id,refresh}=useBusiness(services);const title=section==='intelligence'?'Business Intelligence':section==='engage'?'Engage':section==='analytics'?'Business Analytics':'Business Overview';const load=async()=>{if(section==='overview')return services.business.listBusinesses();if(!id)return[];if(section==='intelligence')return Promise.all([services.business.locationIntelligence(id),services.business.growthAnalytics(id),services.business.benchmarkAnalytics(id),services.business.occupancyAnalytics(id),services.business.engagementAnalytics(id)]);if(section==='engage')return Promise.all([services.business.listCampaigns(id),services.business.listPromotions(id),services.business.listEvents(id),services.business.listContests(id)]);return Promise.all([services.business.analytics(id),services.business.reviewAnalytics(id),services.business.roiAnalytics(id),services.business.visitorAnalytics(id),services.business.engagementFunnel(id),services.business.campaignAnalytics(id),services.business.promotionAnalytics(id),services.business.qrAnalytics(id),services.business.eventAnalytics(id),services.business.mediaAnalytics(id),services.business.partnerAnalytics(id),services.business.rewardsAnalytics(id),services.business.occupancyAnalytics(id)]);};return <Page workspace="business" title={title}><CapabilityPanel title={title} load={load} renderData={data=><MetricGrid items={data}/>}/>{id&&section==='overview'&&<><CapabilityPanel title="Business locations" load={()=>services.business.listLocations(id)} renderData={data=><MetricGrid items={data}/>}/><ActionForm title="Add location" submitLabel="Create location" fields={[{name:'name',label:'Location name'},{name:'address',label:'Address',required:false},{name:'city',label:'City',required:false}]} onSubmit={async v=>{await services.business.createLocation(id,v);refresh()}}/></>}{id&&section==='engage'&&<><ActionForm title="Create campaign" submitLabel="Create campaign" fields={[{name:'name',label:'Campaign name'},{name:'goal',label:'Goal',required:false},{name:'type',label:'Type',defaultValue:'general',required:false}]} onSubmit={async v=>{await services.business.createCampaign(id,v);refresh()}}/><ActionForm title="Create promotion" submitLabel="Create promotion" fields={[{name:'name',label:'Promotion name'},{name:'description',label:'Description',required:false},{name:'discount',label:'Discount',required:false}]} onSubmit={async v=>{await services.business.createPromotion(id,v);refresh()}}/></>}</Page>}
export function FleetPage({section='operations'}){const{services}=useAppContext();const{id,refresh}=useBusiness(services);const title=({performance:'Performance',opportunities:'Opportunities',goals:'Goals',routes:'Routes',intelligence:'Fleet Intelligence'})[section]||'Fleet Operations';const load=async()=>{if(!id)return[];if(section==='opportunities')return services.fleet.opportunities(id);if(section==='intelligence')return services.fleet.intelligence(id);if(section==='goals')return Promise.all([services.fleetMetrics.configuration(id),services.fleetMetrics.values(id),services.fleetMetrics.leaderboard(id)]);if(section==='performance')return Promise.all([services.fleet.dashboard(id),services.fleetMetrics.values(id),services.fleetMetrics.leaderboard(id)]);return services.fleet.dashboard(id)};return <Page workspace="fleet" title={title}><CapabilityPanel title={title} load={load} renderData={data=><MetricGrid items={data}/>}/>{id&&section==='goals'&&<ActionForm title="Define fleet metric" submitLabel="Create metric" fields={[{name:'metricKey',label:'Metric key'},{name:'featureCode',label:'Feature code'},{name:'name',label:'Metric name'},{name:'sourceDataset',label:'Source dataset'},{name:'sourceMetric',label:'Source metric'},{name:'unit',label:'Unit',required:false},{name:'goal',label:'Goal'}]} onSubmit={async v=>{await services.fleetMetrics.create(id,v);refresh()}}/></Page>}
export function EnterprisePage({section='command'}){const{services,user}=useAppContext();const title=section==='partners'?'Partners':section==='campaigns'?'Campaigns':section==='performance'?'Performance':section==='fleet'?'Fleet':'Enterprise Command';return <Page workspace="enterprise" title={title}><CapabilityPanel title="Partner programs" load={()=>services.partners.list()} renderData={data=><MetricGrid items={data}/>}/><CapabilityPanel title="My partner memberships" load={()=>services.partners.memberships()} renderData={data=><MetricGrid items={data}/>}/>{section==='performance'&&<><ActionForm title="Network analytics" submitLabel="Load network" fields={[{name:'networkId',label:'Network ID'}]} onSubmit={v=>services.partners.network(v.networkId)}/><ActionForm title="Network benchmark" submitLabel="Load benchmark" fields={[{name:'networkId',label:'Network ID'}]} onSubmit={v=>services.partners.benchmarkAnalytics(v.networkId)}/><ActionForm title="Allocation ROI" submitLabel="Load ROI" fields={[{name:'networkId',label:'Network ID'}]} onSubmit={v=>services.partners.allocationRoi(v.networkId)}/></>}{section==='campaigns'&&<><ActionForm title="Create partner campaign" submitLabel="Create campaign" fields={[{name:'networkId',label:'Network ID'},{name:'name',label:'Campaign name'},{name:'campaignType',label:'Campaign type',defaultValue:'engagement',required:false},{name:'goal',label:'Goal',required:false}]} onSubmit={v=>services.partners.createCampaign(v.networkId,v.name,v.campaignType,v.goal||null)}/><ActionForm title="Campaign ROI" submitLabel="Load ROI" fields={[{name:'campaignId',label:'Campaign ID'}]} onSubmit={v=>services.partners.campaignRoi(v.campaignId)}/></>}{section==='fleet'&&user&&<ActionForm title="Enable enterprise fleet service" submitLabel="Enable fleet" fields={[]} onSubmit={()=>services.partners.fleetAccess(user.id)}/>}</Page>}
export function AdminPage(){const{services,profile}=useAppContext();return <Page workspace="admin" title="Platform Command"><CapabilityPanel title="System overview" load={()=>services.admin.overview(profile)} renderData={data=><MetricGrid items={data}/>}/><CapabilityPanel title="Data integrity" load={()=>services.admin.integrity(profile)} renderData={data=><MetricGrid items={data}/>}/><CapabilityPanel title="Pending businesses" load={()=>services.admin.pendingBusinesses(profile)} renderData={data=><MetricGrid items={data}/>}/><CapabilityPanel title="Reports" load={()=>services.admin.reports(profile)} renderData={data=><MetricGrid items={data}/>}/><ActionForm title="Search users" submitLabel="Search" fields={[{name:'query',label:'Search query'}]} onSubmit={v=>services.admin.searchUsers(profile,v.query)}/></Page>}
