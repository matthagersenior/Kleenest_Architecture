import{useEffect,useMemo,useState}from'react';import{Database,RefreshCw,Search,CheckCircle2,AlertTriangle,ShieldCheck,ArrowRight,Layers3,Code2,XCircle}from'lucide-react';import{Link}from'react-router-dom';import{useAppContext}from'../AppContext.jsx';import{getCapabilityRegistry}from'../architecture/capabilityRegistry.js';import WorkspaceShell from'./WorkspaceShell.jsx';import'./OperationalCapabilityPage.css';
const REGISTRY=getCapabilityRegistry();
const label=s=>String(s||'').replaceAll('_',' ').replace(/([a-z])([A-Z])/g,'$1 $2').replace(/\b\w/g,m=>m.toUpperCase());
const routes={identity:'/profile',locations:'/map',discovery:'/discover',maps:'/map',checkins:'/check-in',reviews:'/community',evidence:'/evidence',qr:'/engage',geofencing:'/engagement/orchestrate',progression:'/play',quests:'/play/quest',rewards:'/rewards',reputation:'/profile',leaderboards:'/leaderboards',business:'/business',businessLifecycle:'/business',access:'/access',monetization:'/pricing',fleet:'/fleet',enterprise:'/enterprise',social:'/social',family:'/family',notifications:'/notifications',analytics:'/intelligence',liveNetwork:'/intelligence',intelligence:'/intelligence',reporting:'/owner/reports',externalData:'/admin/crud',offline:'/offline',support:'/support',admin:'/admin'};
const serviceReady=(services,names)=>names.every(name=>Boolean(services?.[name]));
export default function OperationalCapabilityPage(){const{profile,services,isPlatformOwner,loading:authLoading}=useAppContext();const[data,setData]=useState([]),[query,setQuery]=useState(''),[loading,setLoading]=useState(true),[error,setError]=useState('');const ready=!authLoading&&isPlatformOwner&&!!profile;const load=async()=>{if(!ready){setLoading(false);return}setLoading(true);setError('');try{const v=await services.admin.operationalCapabilityCatalog(profile);setData(Array.isArray(v)?v:(v?.items||v?.rows||v?.data||[]))}catch(e){setData([]);setError(e.message||'Unable to load operational capability catalog.')}finally{setLoading(false)}};useEffect(()=>{if(authLoading)return;if(ready)void load();else setLoading(false)},[authLoading,isPlatformOwner,profile,services]);const backend=new Map(data.map(x=>[String(x.resource||x.feature_code||''),x]));const rows=useMemo(()=>Object.entries(REGISTRY).map(([id,cap])=>{const catalog=backend.get(id),coverage=serviceReady(services,cap.services||[]);return{id,...cap,label:cap.label||label(id),catalog,coverage,route:routes[id]||null,status:catalog?.status||'unverified_runtime'}}),[services,data]);const filtered=useMemo(()=>rows.filter(x=>!query||`${x.id} ${x.label} ${x.domain||''} ${(x.facts||[]).join(' ')} ${(x.services||[]).join(' ')} ${x.catalog?.status||''}`.toLowerCase().includes(query.toLowerCase())),[rows,query]);const wired=data.filter(x=>x.status==='wired'),gaps=data.filter(x=>x.status==='backend_present_ui_gap'),missing=data.filter(x=>x.status==='missing_backend'),unverified=rows.filter(x=>!x.catalog);if(authLoading)return <WorkspaceShell workspace="owner"><section className="empty-state"><ShieldCheck size={28}/><h2>Loading capability catalog</h2><p>Waiting for the authenticated owner session.</p></section></WorkspaceShell>;if(!isPlatformOwner)return <WorkspaceShell workspace="consumer"><section className="empty-state"><ShieldCheck size={28}/><h2>Capability catalog</h2><p>Platform owner access is required.</p></section></WorkspaceShell>;return <WorkspaceShell workspace="owner"><section className="page capability-operations"><div className="page-header"><div><span className="eyebrow">CANONICAL CAPABILITY REGISTRY</span><h1>Operational Capabilities</h1><p>Live operational state comes from the protected Supabase catalog. The code registry identifies expected capabilities but never overrides backend state.</p></div><div className="hero-actions"><Link className="button secondary" to="/admin/capabilities"><Layers3 size={16}/>Capability Hub</Link><button className="button secondary" onClick={load} disabled={loading}><RefreshCw size={16}/>{loading?'Refreshing…':'Refresh catalog'}</button></div></div>{error&&<p className="form-error" role="alert">{error}</p>}<section className="reward-stats"><div className="reward-stat"><Layers3 size={18}/><strong>{data.length}</strong><span>backend records</span></div><div className="reward-stat"><CheckCircle2 size={18}/><strong>{wired.length}</strong><span>backend wired</span></div><div className="reward-stat"><AlertTriangle size={18}/><strong>{gaps.length}</strong><span>UI gaps</span></div><div className="reward-stat"><XCircle size={18}/><strong>{missing.length}</strong><span>missing backend</span></div></section><section className="detail-panel"><div className="hero-actions"><Search size={17}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search capability, service, fact, or workspace" aria-label="Search capabilities"/></div></section><section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">RECONCILIATED VIEW</span><h2>{filtered.length} capabilities</h2></div><Code2 size={21}/></div><div className="business-list">{filtered.map(x=><div className="business-row" key={x.id}><div><strong>{x.label}</strong><span>{label(x.id)} · {x.catalog?label(x.status):'NOT VERIFIED'} · {x.services?.length||0} services · {x.facts?.length||0} facts</span><small>{x.catalog?`${x.catalog.exists===false?'Backend resource missing.':x.catalog.estimated_rows==null?'Backend resource exists; row count unavailable.':`Backend resource exists; ~${Math.max(0,Number(x.catalog.estimated_rows)).toLocaleString()} estimated rows.`}`:'No authoritative backend catalog record matches this capability.'}</small></div><div className="hero-actions"><span className={`tag ${x.status==='wired'?'active':''}`}>{label(x.status)}</span>{x.route&&<Link className="button secondary" to={x.route}>Open <ArrowRight size={14}/></Link>}</div></div>)}{!filtered.length&&!loading&&<div className="empty-state"><Search size={20}/><strong>No matching capabilities</strong></div>}</div></section><section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">LIVE BACKEND EVIDENCE</span><h2>{data.length} catalog records</h2></div><Database size={21}/></div>{data.length?<div className="business-list">{data.slice(0,200).map((x,i)=><div className="business-row" key={`${x.resource||x.feature_code||'record'}-${i}`}><div><strong>{label(x.feature_code||x.resource)}</strong><span>{label(x.workspace)} · {label(x.category)} · {x.exists===false?'backend resource missing':'backend resource present'} · {x.estimated_rows==null?'row count unavailable':`${Math.max(0,Number(x.estimated_rows)).toLocaleString()} estimated rows`}</span></div><span className={`tag ${x.status==='wired'?'active':''}`}>{label(x.status||'unknown')}</span></div>)}</div>:<div className="empty-state"><Database size={20}/><strong>{loading?'Loading live catalog…':'No backend catalog records returned'}</strong><span>{error?'Backend query failed; no state is inferred.':'The protected catalog returned no records.'}</span></div>}</section>{unverified.length>0&&<section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">RECONCILIATION</span><h2>{unverified.length} capabilities need backend verification</h2></div><AlertTriangle size={21}/></div><p>These code-registry expectations are deliberately not counted as wired until the backend catalog reports them.</p></section>}<small>Authority: admin_operational_capability_catalog() → live Supabase schema evidence.</small></section></WorkspaceShell>}
