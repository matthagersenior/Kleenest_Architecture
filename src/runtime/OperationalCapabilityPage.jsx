import{useEffect,useMemo,useState}from'react';import{Database,RefreshCw,Search,CheckCircle2,AlertTriangle,ShieldCheck,ArrowRight,Layers3,Code2}from'lucide-react';import{Link}from'react-router-dom';import{useAppContext}from'../AppContext.jsx';import{getCapabilityRegistry}from'../architecture/capabilityRegistry.js';import WorkspaceShell from'./WorkspaceShell.jsx';import'./OperationalCapabilityPage.css';
const REGISTRY=getCapabilityRegistry();
const label=s=>String(s||'').replaceAll('_',' ').replace(/([a-z])([A-Z])/g,'$1 $2').replace(/\b\w/g,m=>m.toUpperCase());
const routes={identity:'/profile',locations:'/map',discovery:'/discover',maps:'/map',checkins:'/check-in',reviews:'/community',evidence:'/evidence',qr:'/engage',geofencing:'/engagement/orchestrate',progression:'/play',quests:'/play/quest',rewards:'/rewards',reputation:'/profile',leaderboards:'/leaderboards',business:'/business',businessLifecycle:'/business',access:'/access',monetization:'/pricing',fleet:'/fleet',enterprise:'/enterprise',social:'/social',family:'/family',notifications:'/notifications',analytics:'/intelligence',liveNetwork:'/intelligence',intelligence:'/intelligence',reporting:'/owner/reports',externalData:'/admin/crud',offline:'/offline',support:'/support',admin:'/admin'};
const serviceReady=(services,names)=>names.every(name=>Boolean(services?.[name]));
export default function OperationalCapabilityPage(){const{profile,services,isPlatformOwner,loading:authLoading}=useAppContext();const[data,setData]=useState([]),[query,setQuery]=useState(''),[loading,setLoading]=useState(true),[error,setError]=useState('');const ready=!authLoading&&isPlatformOwner&&!!profile;const load=async()=>{if(!ready){setLoading(false);return}setLoading(true);setError('');try{const v=services?.admin?.operationalCapabilityCatalog?await services.admin.operationalCapabilityCatalog(profile):[];setData(Array.isArray(v)?v:(v?.rows||v?.data||[]))}catch(e){setData([]);setError(e.message||'Unable to load operational catalog telemetry.')}finally{setLoading(false)}};useEffect(()=>{if(authLoading)return;if(ready)void load();else setLoading(false)},[authLoading,isPlatformOwner,profile,services]);const rows=useMemo(()=>Object.entries(REGISTRY).map(([id,cap])=>{const catalog=data.find(x=>x.feature_code===id||x.resource===id);const coverage=serviceReady(services,cap.services||[]);const enabled=catalog?.enabled!==false;return{id,...cap,label:cap.label||label(id),coverage,enabled,catalog,route:routes[id]||null,status:!coverage?'MISSING SERVICE':!enabled?'DISABLED':'WIRED'}}),[services,data]);const filtered=useMemo(()=>rows.filter(x=>!query||`${x.id} ${x.label} ${x.domain||''} ${(x.facts||[]).join(' ')} ${(x.services||[]).join(' ')}`.toLowerCase().includes(query.toLowerCase())),[rows,query]);const missing=rows.filter(x=>!x.coverage),disabled=rows.filter(x=>!x.enabled),wired=rows.filter(x=>x.coverage&&x.enabled);if(authLoading)return <WorkspaceShell workspace="owner"><section className="empty-state"><ShieldCheck size={28}/><h2>Loading capability catalog</h2><p>Waiting for the authenticated owner session before querying protected capability metadata.</p></section></WorkspaceShell>;if(!isPlatformOwner)return <WorkspaceShell workspace="consumer"><section className="empty-state"><ShieldCheck size={28}/><h2>Capability catalog</h2><p>Platform owner access is required.</p></section></WorkspaceShell>;return <WorkspaceShell workspace="owner"><section className="page capability-operations"><div className="page-header"><div><span className="eyebrow">CANONICAL CAPABILITY REGISTRY</span><h1>Operational Capabilities</h1><p>The live owner view of what Kleenest can do: authoritative services, facts, workspace exposure, runtime coverage, and current UI entry points.</p></div><div className="hero-actions"><Link className="button secondary" to="/admin/capabilities"><Layers3 size={16}/>Capability Hub</Link><button className="button secondary" onClick={load} disabled={loading}><RefreshCw size={16}/>{loading?'Refreshing…':'Refresh telemetry'}</button></div></div>{error&&<p className="form-error" role="alert">{error}</p>}<section className="reward-stats"><div className="reward-stat"><Layers3 size={18}/><strong>{rows.length}</strong><span>canonical capabilities</span></div><div className="reward-stat"><CheckCircle2 size={18}/><strong>{wired.length}</strong><span>runtime covered</span></div><div className="reward-stat"><AlertTriangle size={18}/><strong>{missing.length}</strong><span>missing services</span></div><div className="reward-stat"><Database size={18}/><strong>{data.length}</strong><span>telemetry records</span></div></section><section className="detail-panel"><div className="hero-actions"><Search size={17}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search capability, service, fact, or workspace" aria-label="Search capabilities"/></div></section><section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">LIVE REGISTRY</span><h2>{filtered.length} capabilities</h2></div><Code2 size={21}/></div><div className="business-list">{filtered.map(x=><div className="business-row" key={x.id}><div><strong>{x.label}</strong><span>{label(x.id)} · {x.status} · {x.services?.length||0} services · {x.facts?.length||0} authoritative facts · {(x.ui||[]).join(', ')||'no UI scope'}</span><small>{x.flow||x.rule||'Canonical capability contract.'}</small></div><div className="hero-actions"><span className={`tag ${x.status==='WIRED'?'active':''}`}>{x.status}</span>{x.route&&<Link className="button secondary" to={x.route}>Open <ArrowRight size={14}/></Link>}</div></div>)}{!filtered.length&&!loading&&<div className="empty-state"><Search size={20}/><strong>No matching capabilities</strong></div>}</div></section><section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">TELEMETRY RECONCILIATION</span><h2>Backend catalog records</h2></div><Database size={21}/></div>{data.length?<div className="business-list">{data.slice(0,100).map((x,i)=><div className="business-row" key={`${x.resource||x.feature_code||'record'}-${i}`}><div><strong>{label(x.feature_code||x.resource)}</strong><span>{label(x.workspace)} · {label(x.category)} · {x.exists===false?'dataset missing':'dataset present'} · {x.estimated_rows==null?'—':Math.max(0,Number(x.estimated_rows)).toLocaleString()} rows</span></div><span className="eyebrow">{x.status||'TELEMETRY'}</span></div>)}</div>:<div className="empty-state"><Database size={20}/><strong>No separate telemetry records returned</strong><span>The canonical registry remains authoritative.</span></div>}</section>{disabled.length>0&&<section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">CATALOG OVERRIDES</span><h2>{disabled.length} disabled capabilities</h2></div><AlertTriangle size={21}/></div><p>Disabled catalog entries are surfaced here for owner investigation; client visibility does not override backend authorization.</p></section>}<small>Registry authority: Supabase-backed service contracts · owner control plane</small></section></WorkspaceShell>}
