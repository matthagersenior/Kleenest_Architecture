import{useEffect,useState}from'react';import{useNavigate,Link}from'react-router-dom';import{Settings,ShieldCheck}from'lucide-react';import{useAppContext}from'../AppContext.jsx';importWorkspaceShellfrom'./WorkspaceShell.jsx';export default function ProfilePage(){const{services,user,profile,entitlements,billingEntitlements,membershipTier,showAds,isPlatformOwner,loading,error,serviceError}=useAppContext();const nav=useNavigate();const[name,setName]=useState(''),[username,setUsername]=useState(''),[bio,setBio]=useState(''),[password,setPassword]=useState(''),[status,setStatus]=useState('');useEffect(()=>{setName(profile?.display_name??'');setUsername(profile?.username??'');setBio(profile?.bio??'')},[profile]);const save=async()=>{if(!user){nav('/auth');return}try{setStatus('Saving…');const next=await services.profile.update(user.id,{display_name:name,username,bio});await services.identity.updateUserMetadata({full_name:name});setName(next?.display_name??name);setUsername(next?.username??username);setBio(next?.bio??bio);setStatus('Profile saved.')}catch(e){setStatus(e.message||'Unable to save profile.')}};const change=async()=>{try{await services.identity.updatePassword(password);setPassword('');setStatus('Password updated.')}catch(e){setStatus(e.message||'Unable to update password.')}};if(loading)return <WorkspaceShell><section className="page-head"><span className="eyebrow">Profile</span><h1>Loading your account…</h1><p>Restoring your authenticated Kleenest identity.</p></section></WorkspaceShell>;if(!user)return <WorkspaceShell><section className="page-head auth-required"><span className="eyebrow">Profile</span><h1>Sign in to your account</h1><p>Your membership, owner access, progress, and saved data appear here after authentication.</p>{error&&<div className="result-card"><strong>Authentication state unavailable</strong><span>{error.message||'Please sign in again.'}</span></div>}<button type="button" className="button primary" onClick={()=>nav('/auth')}>Sign in</button></section></WorkspaceShell>;return <WorkspaceShell workspace={isPlatformOwner?'owner':undefined}><section className="page-head profile-page"><span className="eyebrow">{isPlatformOwner?'OWNER ACCOUNT':'PROFILE'}</span><h1>{name||user.email||'Your account'}</h1><p>{isPlatformOwner?'Your authenticated platform-owner identity and private control-plane access.':'Manage identity, membership, privacy, and your Kleenest experience.'}</p><article className="result-card"><strong>{isPlatformOwner?'Platform Owner':'Membership: '+membershipTier}</strong><span>{isPlatformOwner?'Owner access is active. Your account is not treated as a Free consumer membership.':'Membership: '+membershipTier+'. '+(showAds?'Freemium ads are enabled.':'Ads are removed for this membership.')}</span>{serviceError&&<small>Some optional account data is still loading.</small>}{isPlatformOwner&&<div className="hero-actions"><button type="button" className="button primary" onClick={()=>nav('/owner')}>Open Owner Control Center</button><button type="button" className="button" onClick={()=>nav('/admin/capabilities')}>Capability Registry</button></div>}</article><div className="hero-actions"><Link className="button secondary" to="/profile/preferences"><Settings size={16}/>Privacy & preferences</Link><Link className="button secondary" to="/notifications/preferences"><ShieldCheck size={16}/>Notification preferences</Link></div><div className="profile-form"><label>Display name<input value={name} onChange={e=>setName(e.target.value)}/></label><label>Username<input value={username} onChange={e=>setUsername(e.target.value)}/></label><label>Bio<textarea value={bio} onChange={e=>setBio(e.target.value)}/></label><button type="button" className="button primary" onClick={save}>Save profile</button><label>New password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="new-password"/></label><button type="button" className="button" disabled={password.length<8} onClick={change}>Change password</button></div><div className="profile-stats"><div><strong>Progress</strong><p>{profile?.points??0} points · Level {profile?.level??1} · {profile?.streak??0} day streak</p></div><div><strong>Entitlements</strong><p>{[...(entitlements||[]),...(billingEntitlements||[])].length} active entitlement records.</p></div></div><span role="status">{status}</span></section></WorkspaceShell>}