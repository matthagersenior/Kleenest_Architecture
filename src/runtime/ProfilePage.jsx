import {useState} from 'react';
import {useAppContext} from '../AppContext.jsx';
import WorkspaceShell from './WorkspaceShell.jsx';
export default function ProfilePage(){const {services,user,profile,entitlements,membershipTier,showAds}=useAppContext();const [name,setName]=useState(profile?.display_name??'');const [username,setUsername]=useState(profile?.username??'');const [bio,setBio]=useState(profile?.bio??'');const [password,setPassword]=useState('');const [status,setStatus]=useState('');const save=async()=>{try{setStatus('Saving…');await services.identity.updateUserMetadata({full_name:name});setStatus('Account details saved. Profile identity is managed by Supabase Auth; extended profile fields remain the canonical profiles record.')}catch(e){setStatus(e.message)}};const change=async()=>{try{await services.identity.updatePassword(password);setPassword('');setStatus('Password updated.')}catch(e){setStatus(e.message)}};return <WorkspaceShell><section className="page-head"><span className="eyebrow">Profile</span><h1>{name||user?.email||'Your account'}</h1><p>Manage identity, membership, privacy, and your Kleenest experience.</p><article className="result-card"><strong>Membership: {membershipTier}</strong><span>{showAds?'Freemium ads are enabled.':'Ads are removed for this membership.'}</span></article><label>Display name<input value={name} onChange={e=>setName(e.target.value)} /></label><label>Username<input value={username} onChange={e=>setUsername(e.target.value)} /></label><label>Bio<textarea value={bio} onChange={e=>setBio(e.target.value)} /></label><button className="button primary" onClick={save}>Save identity</button><label>New password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} /></label><button className="button" disabled={!password} onClick={change}>Change password</button><div><strong>Progress</strong><p>{profile?.points??0} points · Level {profile?.level??1} · {profile?.streak??0} day streak</p></div><div><strong>Entitlements</strong><p>{entitlements?.length??0} active entitlement records.</p></div><span>{status}</span></section></WorkspaceShell>}
