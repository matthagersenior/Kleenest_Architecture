import{useState}from'react';import{Check,ShieldCheck,Building2,Users}from'lucide-react';import{useAppContext}from'../AppContext.jsx';
const USER_TIERS=[['free','Free'],['premium','Premium'],['family','Family'],['fleet','Fleet User'],['enterprise','Enterprise User']];
const BUSINESS_TIERS=[['standard','Business Standard'],['growth','Business Growth'],['fleet','Business Fleet'],['enterprise','Business Enterprise']];
export default function OwnerMembershipControls(){const{profile,services,isPlatformOwner}=useAppContext();const[q,setQ]=useState('');const[users,setUsers]=useState([]);const[selected,setSelected]=useState(null);const[userTier,setUserTier]=useState('free');const[businessId,setBusinessId]=useState('');const[businessTier,setBusinessTier]=useState('standard');const[busy,setBusy]=useState(false);const[error,setError]=useState('');const[message,setMessage]=useState('');if(!isPlatformOwner)return null;const search=async()=>{if(!q.trim())return;setBusy(true);setError('');setMessage('');try{const rows=await services.admin.searchUsers(profile,q.trim());setUsers(Array.isArray(rows)?rows:[])}catch(e){setError(e.message||'Unable to search accounts.')}finally{setBusy(false)}};const selectUser=u=>{setSelected(u);setUserTier(String(u.subscription_tier||'free').toLowerCase())};const saveUser=async()=>{if(!selected?.id)return;if(!window.confirm(`Change membership tier for ${selected.email||selected.name||selected.id} to ${userTier}?`))return;setBusy(true);setError('');setMessage('');try{await services.admin.setAccountCapabilities(profile,selected.id,{subscriptionTier:userTier,reason:`Owner membership tier change to ${userTier}`});setMessage(`User membership changed to ${userTier}.`);const rows=await services.admin.searchUsers(profile,q.trim());setUsers(Array.isArray(rows)?rows:[]);setSelected((Array.isArray(rows)?rows:[]).find(x=>x.id===selected.id)||null)}catch(e){setError(e.message||'Unable to change user membership tier.')}finally{setBusy(false)}};const saveBusiness=async()=>{if(!businessId.trim())return;if(!window.confirm(`Change business ${businessId} to ${businessTier}?`))return;setBusy(true);setError('');setMessage('');try{await services.admin.setBusinessTier(profile,businessId.trim(),businessTier);setMessage(`Business tier changed to ${businessTier}.`)}catch(e){setError(e.message||'Unable to change business tier.')}finally{setBusy(false)}};return <section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">MEMBERSHIP CONTROL</span><h2>Change membership tiers</h2></div><ShieldCheck size={22}/></div><p>Owner-only controls for assigning consumer membership tiers and business subscription tiers. Changes use the governed admin RPCs and are audited by the backend.</p>{error&&<p className="form-error" role="alert">{error}</p>}{message&&<p className="form-success" role="status">{message}</p>}<div className="dashboard-grid"><article className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">CONSUMER ACCOUNTS</span><h3><Users size={18}/> User membership</h3></div></div><div className="hero-actions"><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&search()} placeholder="Email, name, or user ID"/><button className="button primary" onClick={search} disabled={busy||!q.trim()}>Search</button></div>{users.length>0&&<div className="business-list">{users.map(u=><button type="button" className={`business-row${selected?.id===u.id?' selected':''}`} key={u.id} onClick={()=>selectUser(u)}><div><strong>{u.email||u.name||u.id}</strong><span>{u.id} · {u.subscription_tier||'free'}</span></div></button>)}</div>}{selected&&<><label>Membership tier<select value={userTier} onChange={e=>setUserTier(e.target.value)}>{USER_TIERS.map(([id,label])=><option key={id} value={id}>{label}</option>)}</select></label><div className="hero-actions"><button className="button primary" onClick={saveUser} disabled={busy}><Check size={16}/>Save user tier</button></div></>}</article><article className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">BUSINESS ACCOUNTS</span><h3><Building2 size={18}/> Business owner tier</h3></div></div><label>Business ID<input value={businessId} onChange={e=>setBusinessId(e.target.value)} placeholder="Business UUID"/></label><label>Business tier<select value={businessTier} onChange={e=>setBusinessTier(e.target.value)}>{BUSINESS_TIERS.map(([id,label])=><option key={id} value={id}>{label}</option>)}</select></label><div className="hero-actions"><button className="button primary" onClick={saveBusiness} disabled={busy||!businessId.trim()}><Check size={16}/>Save business tier</button></div></article></div></section>}
