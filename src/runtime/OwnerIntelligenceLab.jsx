import{useEffect,useMemo,useState}from'react';import{Activity,ArrowRight,Brain,Building2,CheckCircle2,Gauge,Lightbulb,RefreshCw,Target,Truck,Globe2}from'lucide-react';import{Link}from'react-router-dom';import{useAppContext}from'../AppContext.jsx';import WorkspaceShell from'./WorkspaceShell.jsx';import'./OwnerIntelligenceLab.css';
const scopes=[['business','Business','Growth, engagement, campaigns, ROI and recommendations.',Building2],['fleet','Fleet','Operational metrics, service opportunities and performance.',Truck],['enterprise','Enterprise','Network metrics, campaign ROI and partner allocation.',Globe2]];
const safeRows=v=>Array.isArray(v)?v:(v&&Array.isArray(v.rows)?v.rows:[]);const titleOf=x=>x?.title||x?.name||x?.label||x?.metric_name||x?.metric_key||'Intelligence item';const detailOf=x=>x?.description||x?.summary||x?.detail||x?.body||x?.status||'Live intelligence data';
export default function OwnerIntelligenceLab(){const{services,isPlatformOwner}=useAppContext();const[scope,setScope]=useState('business');const[id,setId]=useState('');const[data,setData]=useState([]);const[busy,setBusy]=useState(false);const[error,setError]=useState('');const[ran,setRan]=useState(false);const config=useMemo(()=>({business:[['Growth analytics','businessGrowthAnalytics'],['Engagement','businessEngagementAnalytics'],['Campaigns','businessCampaignAnalytics'],['ROI','businessRoiAnalytics'],['Benchmarks','businessBenchmarkAnalytics'],['Growth actions','businessGrowthActions']],fleet:[['Dashboard','fleetDashboardSummary'],['Capabilities','fleetMetricCapabilities'],['Service opportunities','fleetServiceOpportunities']],enterprise:[['Network metrics','enterpriseNetworkMetrics'],['Campaign ROI','enterpriseCampaignRoi'],['Partner ROI','enterprisePartnerRoi']]}),[]);const run=async()=>{if(!id.trim()){setError('Enter the authoritative entity ID for this intelligence scope.');return}setBusy(true);setError('');setRan(false);try{const out=[];for(const[item,method]of config[scope]){try{const value=await services.intelligenceOwner[method](id.trim());out.push({item,value})}catch(e){out.push({item,error:e.message||'Unavailable'})}}setData(out);setRan(true)}catch(e){setError(e.message||'Unable to load intelligence.')}finally{setBusy(false)}};useEffect(()=>{const onUpdate=()=>{if(ran)void run()};window.addEventListener('kleenest:intelligence-updated',onUpdate);return()=>window.removeEventListener('kleenest:intelligence-updated',onUpdate)},[scope,id,ran]);if(!isPlatformOwner)return <WorkspaceShell workspace="consumer"><section className="empty-state"><Brain size={28}/><h2>Intelligence Lab</h2><p>This owner operating surface is restricted to the platform owner.</p></section></WorkspaceShell>;return <WorkspaceShell workspace="owner"><section className="page owner-intelligence-lab"><div className="page-header"><div><span className="eyebrow">OWNER PRODUCT LAB 3</span><h1>Intelligence Lab</h1><p>Observe the live intelligence stack from signal and analytics through opportunities, actions, and outcomes.</p></div><Link className="button secondary" to="/owner"><ArrowRight size={16}/>Back to Command Center</Link></div><section className="intelligence-flow"><div><Activity size={19}/><strong>Signals</strong><span>Live platform evidence</span></div><ArrowRight/><div><Gauge size={19}/><strong>Analytics</strong><span>Measured performance</span></div><ArrowRight/><div><Lightbulb size={19}/><strong>Opportunities</strong><span>Recommended action</span></div><ArrowRight/><div><Target size={19}/><strong>Actions</strong><span>Operational response</span></div><ArrowRight/><div><CheckCircle2 size={19}/><strong>Outcomes</strong><span>Closed-loop learning</span></div></section><section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">1 · CHOOSE SCOPE</span><h2>Intelligence domains</h2></div><Brain size={23}/></div><div className="scope-grid">{scopes.map(([key,label,desc,Icon])=><button key={key} className={`scope-card${scope===key?' active':''}`} onClick={()=>{setScope(key);setData([]);setRan(false);setError('')}}><Icon size={22}/><strong>{label}</strong><span>{desc}</span></button>)}</div><div className="lab-form"><label><span>{scope==='business'?'Business':scope==='fleet'?'Fleet':'Enterprise'} ID</span><input value={id} onChange={e=>setId(e.target.value)} onKeyDown={e=>e.key==='Enter'&&run()} placeholder="Enter authoritative ID"/></label><button className="button primary" onClick={run} disabled={busy||!id.trim()}><RefreshCw size={16}/>{busy?'Loading intelligence…':'Load live intelligence'}</button></div>{error&&<p className="form-error" role="alert">{error}</p>}</section>{ran&&<section className="intelligence-results"><div className="panel-heading"><div><span className="eyebrow">2 · LIVE RESULTS</span><h2>{scope==='business'?'Business':scope==='fleet'?'Fleet':'Enterprise'} intelligence</h2></div><span className="live-badge">LIVE</span></div><div className="result-grid">{data.map(({item,value,error})=><article className="result-card" key={item}><div><span className="eyebrow">{item}</span>{error?<p className="result-error">{error}</p>:safeRows(value).length>0?<><strong>{safeRows(value).length} records</strong><div className="result-list">{safeRows(value).slice(0,4).map((x,i)=><div key={i}><strong>{titleOf(x)}</strong><span>{detailOf(x)}</span></div>)}</div></>:<pre>{JSON.stringify(value,null,2)}</pre>}</div></article>)}</div></section>}<section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">3 · ACTION LOOP</span><h2>Close the loop</h2><p>Use the existing governed action surface to execute and complete intelligence actions, then record outcomes for the feedback loop.</p></div><Target size={22}/></div><div className="hero-actions"><Link className="button primary" to="/owner/intelligence/actions"><Target size={16}/>Open action queue</Link><Link className="button secondary" to="/owner/reports"><Activity size={16}/>Reporting</Link></div></section></section></WorkspaceShell>}
