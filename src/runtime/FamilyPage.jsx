import {useState} from 'react';import {CheckCircle2,Heart,Mail,RefreshCw,Users} from 'lucide-react';import WorkspaceShell from './WorkspaceShell.jsx';import {useAppContext} from '../AppContext.jsx';
export default function FamilyPage(){const{services}=useAppContext();const[name,setName]=useState('');const[email,setEmail]=useState('');const[access,setAccess]=useState(null);const[result,setResult]=useState(null);const[busy,setBusy]=useState('');const[error,setError]=useState('');const run=async(key,fn)=>{setBusy(key);setError('');try{const data=await fn();setResult(data);if(key==='access')setAccess(data);return data}catch(e){setError(e.message||'Family action failed.')}finally{setBusy('')}};return <WorkspaceShell workspace="consumer"><section className="page business-page"><div className="page-header"><div><span className="eyebrow">FAMILY</span><h1>Family access</h1><p>Share premium access and coordinate a safer, simpler Kleenest experience with your household.</p></div><button className="secondary" disabled={busy!==''} onClick={()=>run('access',()=>services.family.hasPremiumAccess())}><RefreshCw size={16}/>Check access</button></div>{error&&<div className="state error" role="alert">{error}</div>}<section className="reward-stats"><div className="reward-stat"><Users size={20}/><strong>Family</strong><span>workspace</span></div><div className="reward-stat"><CheckCircle2 size={20}/><strong>{access===null?'—':access?'Active':'Not active'}</strong><span>premium access</span></div><div className="reward-stat"><Heart size={20}/><strong>Shared</strong><span>experience</span></div></section><div className="business-grid"><section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">HOUSEHOLD</span><h2>Create your family</h2></div><Users size={22}/></div><label>Family name<input value={name} onChange={e=>setName(e.target.value)} placeholder="The Smith family"/></label><button className="primary" disabled={busy!==''||!name.trim()} onClick={()=>run('create',()=>services.family.createGroup(name))}>{busy==='create'?'Creating…':'Create family group'}</button></section><section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">INVITE</span><h2>Add a member</h2></div><Mail size={22}/></div><label>Member email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="member@example.com"/></label><div className="hero-actions"><button className="primary" disabled={busy!==''||!email.trim()} onClick={()=>run('invite',()=>services.family.inviteMember(email))}>{busy==='invite'?'Sending…':'Send invitation'}</button><button className="secondary" disabled={busy!==''} onClick={()=>run('accept',()=>services.family.acceptInvite())}>{busy==='accept'?'Accepting…':'Accept pending invite'}</button></div></section></div>{result&&<section className="detail-panel"><div className="panel-heading"><h2>Latest family action</h2></div><pre className="data-preview">{JSON.stringify(result,null,2)}</pre></section>}</section></WorkspaceShell>}
