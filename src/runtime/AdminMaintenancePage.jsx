import { useState } from 'react';
import { Link } from 'react-router-dom';
import { MapPinned, Settings2, Radio, Users, Building2, Wrench } from 'lucide-react';
import { useAppContext } from '../AppContext.jsx';
import WorkspaceShell from './WorkspaceShell.jsx';
const MARKETS=[['STL','St. Louis'],['KCMO','Kansas City'],['NYC','New York City'],['LA','Los Angeles'],['CHI','Chicago'],['DFW','Dallas–Fort Worth'],['HOU','Houston'],['DC','Washington, DC'],['MIA','Miami'],['PHL','Philadelphia']];
export default function AdminMaintenancePage(){
 const {services,profile}=useAppContext(); const [busy,setBusy]=useState(''); const [result,setResult]=useState(null); const [open,setOpen]=useState(false);
 const run=async(key,fn)=>{setBusy(key);setResult(null);try{setResult(await fn())}catch(error){setResult({ok:false,error:error.message||String(error)})}finally{setBusy('')}};
 const ingest=(key,source,market)=>run(key,()=>services.admin.ingestMarket(profile,source,market));
 return <WorkspaceShell workspace="admin"><section className="page-heading"><span className="eyebrow">ADMINISTRATION</span><h1>Admin tools</h1><p>Operate the Kleenest network. Ingestion stays secondary to administration.</p></section><div className="detail-grid"><section className="detail-panel"><h2>Network operations</h2><div className="action-row"><Link className="button secondary" to="/admin/crud"><Settings2 size={18}/> Network CRUD</Link><Link className="button secondary" to="/fleet"><Radio size={18}/> Fleet operations</Link><Link className="button secondary" to="/business/dashboard"><Building2 size={18}/> Business operations</Link><Link className="button secondary" to="/map"><MapPinned size={18}/> Network map</Link><Link className="button secondary" to="/notifications"><Users size={18}/> Community activity</Link></div></section><section className="detail-panel"><h2>Diagnostics</h2><div className="action-row"><button disabled={!!busy} onClick={()=>run('health',()=>services.admin.health(profile))}>{busy==='health'?'Checking…':'Backend health'}</button><button disabled={!!busy} onClick={()=>run('refresh',()=>services.admin.refreshDerived(profile))}>{busy==='refresh'?'Refreshing…':'Refresh derived data'}</button><button disabled={!!busy} onClick={()=>run('quality',()=>services.admin.locationQuality(profile))}>{busy==='quality'?'Auditing…':'Location quality audit'}</button><button disabled={!!busy} onClick={()=>run('addresses',()=>services.admin.backfillAddresses(profile,25))}>{busy==='addresses'?'Backfilling…':'Backfill 25 addresses'}</button></div></section><section className="detail-panel"><button type="button" className="admin-collapse" aria-expanded={open} onClick={()=>setOpen(v=>!v)}><span><span className="eyebrow">ADVANCED / DATA</span><h2>Data ingestion</h2><p>Run OSM and Data.gov independently by market.</p></span><Wrench size={22}/></button>{open&&<div className="market-grid"><div className="action-row"><button disabled={!!busy} onClick={()=>run('osm-top',()=>services.admin.ingestTopMarkets(profile,'osm'))}>{busy==='osm-top'?'Running…':'OSM top markets'}</button><button disabled={!!busy} onClick={()=>run('data-top',()=>services.admin.ingestTopMarkets(profile,'datagov'))}>{busy==='data-top'?'Running…':'Data.gov top markets'}</button></div>{MARKETS.map(([code,label])=><div className="detail-panel" key={code}><strong>{label}</strong><span>{code}</span><div className="action-row"><button disabled={!!busy} onClick={()=>ingest(`${code}-osm`,'osm',code)}>{busy===`${code}-osm`?'Running…':`${label} OSM`}</button><button disabled={!!busy} onClick={()=>ingest(`${code}-data`,'datagov',code)}>{busy===`${code}-data`?'Running…':`${label} Data.gov`}</button><button disabled={!!busy} onClick={()=>run(`${code}-both`,async()=>({osm:await services.admin.ingestMarket(profile,'osm',code),data_gov:await services.admin.ingestMarket(profile,'datagov',code)}))}>{busy===`${code}-both`?'Running…':`${label} both`}</button></div></div>)}</div>}</section></div>{result&&<pre className="detail-panel">{JSON.stringify(result,null,2)}</pre>}</WorkspaceShell>;
}
