import {useEffect,useState}from'react';import{Brain,RefreshCw,Trophy,LockKeyhole}from'lucide-react';import{Link}from'react-router-dom';import{useAppContext}from'../AppContext.jsx';import WorkspaceShell from'./WorkspaceShell.jsx';import CapabilityGate from'./CapabilityGate.jsx';const arr=v=>Array.isArray(v)?v:[],obj=v=>v&&typeof v==='object'?v:{};export default function FleetIntelligenceSurface(){const{services,isPlatformOwner,selectedBusinessId,selectedBusiness}=useAppContext();const[businesses,setBusinesses]=useState([]),[businessId,setBusinessId]=useState(selectedBusinessId||selectedBusiness?.business_id||selectedBusiness?.id||''),[workspace,setWorkspace]=useState(null),[leaders,setLeaders]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState('');const loadBusinesses=async()=>{try{const rows=await services.business.listBusinesses({includeDemo:isPlatformOwner});const next=arr(rows);setBusinesses(next);const selected=selectedBusinessId||selectedBusiness?.business_id||selectedBusiness?.id;setBusinessId(String(selected||next[0]?.business_id||next[0]?.id||''))}catch(e){setError(e.message||'Unable to load business access.')}finally{setLoading(false)}};const load=async()=>{if(!businessId)return;setLoading(true);setError('');try{const[data,leaderboard]=await Promise.all([services.fleetWorkspace.load(businessId),services.fleet.leaderboard(businessId)]);setWorkspace(data);setLeaders(arr(leaderboard))}catch(e){setError(e.message||'Unable to load Fleet intelligence.')}finally{setLoading(false)}};useEffect(()=>{void loadBusinesses()},[isPlatformOwner,selectedBusinessId]);useEffect(()=>{if(businessId)void load()},[businessId]);useEffect(()=>{const refresh=()=>void load();window.addEventListener('kleenest:fleet-updated',refresh);window.addEventListener('kleenest:business-updated',refresh);window.addEventListener('kleenest:activity-updated',refresh);return()=>{window.removeEventListener('kleenest:fleet-updated',refresh);window.removeEventListener('kleenest:business-updated',refresh);window.removeEventListener('kleenest:activity-updated',refresh)}},[businessId]);const dashboard=obj(workspace?.dashboard),opps=arr(workspace?.opportunities),recs=arr(workspace?.recommendations);return <WorkspaceShell workspace="fleet"><section className="page business-page"><div className="page-header"><div><span className="eyebrow">FLEET · INTELLIGENCE</span><h1>Fleet intelligence</h1><p>Operational signals, service opportunities, recommendations, live activity, and ranked performance from the canonical Fleet workspace service.</p></div><div className="hero-actions"><button className="secondary" onClick={load} disabled={loading}><RefreshCw size={16}/>Refresh</button><Link className="secondary" to="/fleet">Operations</Link><Link className="secondary" to="/fleet/performance">Performance</Link></div></div><section className="detail-panel"><label className="form-field"><span>Business</span><select value={businessId} onChange={e=>setBusinessId(e.target.value)}>{businesses.map(b=>{const id=b?.business_id||b?.id;return <option key={id} value={id}>{b?.name||b?.business_name||id}</option>})}</select></label></section>{error&&<p className="form-error" role="alert">{error}</p>}{loading?<div className="empty-state">Loading Fleet intelligence…</div>:<CapabilityGate kind="fleetIntelligence" businessId={businessId} fallback={<div className="empty-state"><LockKeyhole size={32}/><h2>Fleet intelligence unavailable</h2><p>Your current Fleet capability does not include intelligence access.</p><Link className="secondary" to="/fleet">Return to Fleet</Link></div>}><><section className="reward-stats">{Object.entries(dashboard).filter(([,v])=>typeof v!=='object').slice(0,6).map(([k,v])=><div className="reward-stat" key={k}><Brain size={18}/><strong>{String(v)}</strong><span>{k.replaceAll('_',' ')}</span></div>)}</section><section className="detail-panel"><div className="panel-heading"><h2>Service opportunities</h2></div>{opps.length?opps.slice(0,20).map((o,i)=><div className="business-row" key={o?.id||i}><div><strong>{o?.name||o?.title||o?.vehicle_name||'Service opportunity'}</strong><span>{o?.reason||o?.description||o?.details||'Opportunity detected by Fleet intelligence.'}</span></div><span className="muted">{o?.priority||o?.score||''}</span></div>):<p className="muted">No current opportunities.</p>}</section><section className="detail-panel"><div className="panel-heading"><h2>Recommendations</h2></div>{recs.length?recs.slice(0,12).map((r,i)=><div className="business-row" key={`${r.location_id||r.vehicle_id||'signal'}-${i}`}><div><strong>{r.recommendation?.title||r.title||'Fleet recommendation'}</strong><span>{r.name||r.vehicle_name||r.location_name||'Operational signal'} · {(r.recommendation?.reasons||[]).join(', ')||r.recommendation?.body||r.description||'Operational signal detected.'}</span></div><span className="muted">{r.recommendation?.priority||r.priority||'normal'}</span></div>):<p className="muted">No elevated recommendations.</p>}</section><section className="detail-panel"><div className="panel-heading"><h2>Driver leaderboard</h2><Trophy size={22}/></div>{leaders.length?leaders.map((v,i)=><div className="business-row" key={v?.id||v?.target_id||i}><strong>#{v?.rank||i+1} {v?.name||v?.driver_name||v?.target_name||'Driver'}</strong><span>{v?.score??v?.value??v?.metric_value??'—'}</span></div>):<p className="muted">No leaderboard data.</p>}</section></>}</CapabilityGate>}</section></WorkspaceShell>}