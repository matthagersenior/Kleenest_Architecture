import{useEffect,useMemo,useState}from'react';import{ArrowRight,Check,Database,Edit3,Plus,RefreshCw,Search,Trash2,Wand2}from'lucide-react';import{useAppContext}from'../AppContext.jsx';import WorkspaceShell from'./WorkspaceShell.jsx';
const GROUPS={Identity:['profiles','families','family_members'],Business:['businesses','business_campaigns','business_events','promotions','contests','contest_entries','qr_codes'],Locations:['locations','location_hours','amenities','location_amenities','location_fixtures','location_photos'],Enterprise:['enterprise_partner_networks','enterprise_partner_network_members','enterprise_partner_campaigns','enterprise_partner_outcomes','enterprise_partner_allocations'],Fleet:['route_plans','route_stops','route_events'],Engagement:['favorites','follows','social_posts','reviews','check_ins','badges','user_badges'],Operations:['subscriptions','support_requests','notifications','messages','analytics_events']};
const label=s=>String(s).replaceAll('_',' ').replace(/\b\w/g,m=>m.toUpperCase());const readOnlyFields=['id','created_at','updated_at','deleted_at'];
function inputType(c){if(c.type==='boolean')return'checkbox';if(['integer','bigint','numeric','real','double precision'].includes(c.type))return'number';if(c.type==='date')return'date';if(c.type.includes('timestamp'))return'datetime-local';if(c.type==='jsonb'||c.type==='json'||c.type==='ARRAY')return'textarea';return'text'}
function normalizeValue(c,v){if(v===undefined||v===null)return c.type==='boolean'?false:'';if(c.type==='jsonb'||c.type==='json'||c.type==='ARRAY')return typeof v==='string'?v:JSON.stringify(v,null,2);return v}function denormalize(c,v){if(v===''&&c.nullable)return null;if(c.type==='boolean')return!!v;if(['integer','bigint','numeric','real','double precision'].includes(c.type))return v===''?null:Number(v);if(c.type==='jsonb'||c.type==='json'||c.type==='ARRAY'){try{return JSON.parse(v)}catch{throw new Error(`${label(c.name)} must contain valid JSON.`)}}return v}
export default function OwnerCrudWorkbench(){const{profile,services}=useAppContext();const[resource,setResource]=useState('businesses'),[action,setAction]=useState('list'),[catalog,setCatalog]=useState([]),[schema,setSchema]=useState([]),[values,setValues]=useState({}),[result,setResult]=useState(null),[selected,setSelected]=useState(null),[query,setQuery]=useState(''),[loading,setLoading]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState(''),[message,setMessage]=useState('');const meta=useMemo(()=>catalog.find(x=>x.resource===resource)||{resource,read:true,create:false,update:false,delete:false,read_only:true},[catalog,resource]);const allResources=useMemo(()=>catalog.length?catalog:[],[catalog]);const rows=Array.isArray(result)?result:(result?.rows||result?.data||result?[result]:[]);
const loadCatalog=async()=>{setLoading(true);setError('');try{const data=await services.admin.invoke(profile,'admin_crud_capability_catalog',{});setCatalog(Array.isArray(data)?data:(data?.rows||data?.data||[]));}catch(e){setError(e.message||'Unable to load the governed CRUD catalog.')}finally{setLoading(false)}};const loadSchema=async r=>{try{const data=await services.admin.invoke(profile,'admin_crud_schema',{p_resource:r});setSchema(Array.isArray(data)?data:(data?.rows||data?.data||[]))}catch(e){setSchema([]);setError(e.message||'Unable to load field configuration.')}};useEffect(()=>{void loadCatalog()},[]);useEffect(()=>{if(!resource&&allResources[0])setResource(allResources[0].resource);if(resource)void loadSchema(resource);setResult(null);setSelected(null);setValues({});setAction('list')},[resource]);useEffect(()=>{if(!meta.readOnly&&action==='list')void runList();if(meta.read_only||meta.readOnly)setAction('list')},[action,resource,catalog]);
const runList=async()=>{setLoading(true);setError('');try{const v=await services.admin.invoke(profile,'admin_crud_gateway',{p_resource:resource,p_action:'list',p_id:null,p_payload:{}});setResult(v)}catch(e){setError(e.message||'Unable to load records.')}finally{setLoading(false)}};const selectRow=async row=>{setSelected(row);if(row?.id){try{const v=await services.admin.invoke(profile,'admin_crud_gateway',{p_resource:resource,p_action:'get',p_id:row.id,p_payload:{}});const r=v?.data||v;setValues(Object.fromEntries(schema.map(c=>[c.name,normalizeValue(c,r?.[c.name])])))}catch(e){setError(e.message||'Unable to load record.')}}};const setField=(name,v)=>setValues(x=>({...x,[name]:v}));const save=async()=>{setBusy(true);setError('');setMessage('');try{if(action!=='list'&&!meta[action])throw new Error(`${label(resource)} does not support ${action}. Use its protected operation instead.`);const payload={};for(const c of schema){if(readOnlyFields.includes(c.name))continue;if(values[c.name]!==undefined&&values[c.name]!=='')payload[c.name]=denormalize(c,values[c.name]);else if(values[c.name]===null)payload[c.name]=null}const id=selected?.id||null;if((action==='update'||action==='delete')&&!id)throw new Error('Select a record first.');if(!window.confirm(action==='delete'?`Delete this ${label(resource)}?`:`${action==='create'?'Create':'Save'} this ${label(resource)}?`))return;const v=await services.admin.invoke(profile,'admin_crud_gateway',{p_resource:resource,p_action:action,p_id:id,p_payload:payload});setResult(v);setMessage(`${label(resource)} ${action==='create'?'created':action==='delete'?'deleted':'saved'} successfully.`);if(action!=='delete')setSelected(v);else setSelected(null);await runList()}catch(e){setError(e.message||'Operation failed.')}finally{setBusy(false)}};
const visibleRows=rows.filter(r=>!query||JSON.stringify(r).toLowerCase().includes(query.toLowerCase())).slice(0,100);const groups=Object.fromEntries(Object.entries(GROUPS).map(([g,names])=>[g,names.filter(n=>catalog.some(c=>c.resource===n))]));return <WorkspaceShell workspace="admin"><section className="page"><div className="page-header"><div><span className="eyebrow">OWNER OPERATIONS</span><h1>Governed CRUD Control Center</h1><p>The UI is now driven by the Supabase CRUD capability catalog. Read-only resources cannot expose unsafe create/edit/delete controls.</p></div><div className="hero-actions"><button className="button secondary" onClick={loadCatalog} disabled={loading}><RefreshCw size={16}/>Refresh catalog</button></div></div>{error&&<p className="form-error" role="alert">{error}</p>}{message&&<p className="form-success" role="status">{message}</p>}<section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">QUICK CONTROLS</span><h2>Governed resources</h2></div><Wand2 size={22}/></div><div className="hero-actions">{Object.entries(groups).map(([g,rs])=>rs.length?<button className="button secondary" key={g} onClick={()=>{setResource(rs[0]);setAction('list')}}>{g} · {rs.length}</button>:null)}</div></section><section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">RESOURCE</span><h2>{label(resource)}</h2></div><Database size={22}/></div><div className="form-grid"><label>Resource<select value={resource} onChange={e=>setResource(e.target.value)}>{Object.entries(groups).map(([g,rs])=>rs.length?<optgroup key={g} label={g}>{rs.map(r=><option key={r}>{r}</option>)}</optgroup>:null)}</select></label><label>Action<select value={action} onChange={e=>setAction(e.target.value)}><option value="list">Browse</option>{meta.create&&<option value="create">Create</option>}{meta.update&&<option value="update">Edit</option>}{meta.delete&&<option value="delete">Delete</option>}</select></label><label>Search records<input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Filter loaded records"/></label></div>{meta.read_only||meta.readOnly?<p className="form-note">Read-only resource. Use the protected domain operation shown by its owning workspace for mutations.</p>:null}{action!=='create'&&<div className="business-list">{visibleRows.map((r,i)=><button type="button" className={`business-row${selected?.id===r.id?' selected':''}`} key={r.id||i} onClick={()=>selectRow(r)}><div><strong>{r.name||r.title||r.email||r.id||`Record ${i+1}`}</strong><span>{r.id||'Composite/view key'} · {Object.keys(r).length} fields</span></div><ArrowRight size={16}/></button>)}{!visibleRows.length&&<p>No records loaded. Use Refresh or change the resource.</p>}</div>}</section>{(action==='create'||action==='update'||action==='delete')&&<section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">{action.toUpperCase()} · {label(resource)}</span><h2>{selected?`Record ${selected.id}`:'Configure operation'}</h2></div>{action==='create'?<Plus size={22}/>:action==='update'?<Edit3 size={22}/>:<Trash2 size={22}/>}</div>{action!=='delete'&&<div className="form-grid">{schema.filter(c=>!readOnlyFields.includes(c.name)).map(c=>{const t=inputType(c);return <label key={c.name}>{label(c.name)}{c.has_default&&<small>Optional · database default available</small>}{t==='textarea'?<textarea rows={4} value={values[c.name]??''} onChange={e=>setField(c.name,e.target.value)} placeholder={c.type==='jsonb'?'{}':''}/>:t==='checkbox'?<input type="checkbox" checked={!!values[c.name]} onChange={e=>setField(c.name,e.target.checked)}/>:<input type={t} value={values[c.name]??''} onChange={e=>setField(c.name,e.target.value)} placeholder={c.nullable?'Optional':''}/>}</label>})}</div>}{action==='delete'&&<p>This uses the governed Supabase delete operation. Select a record above, then confirm the deletion.</p>}<div className="hero-actions"><button className="button primary" disabled={busy||loading||(action!=='create'&&!selected)} onClick={save}><Check size={16}/>{busy?'Saving…':action==='create'?'Create record':action==='update'?'Save changes':'Delete record'}</button>{selected&&<button className="button secondary" onClick={()=>{setSelected(null);setValues({})}}>Clear selection</button>}</div></section>}</section></WorkspaceShell>}
