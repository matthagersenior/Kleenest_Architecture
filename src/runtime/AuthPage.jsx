import{useState}from'react';import{Eye,EyeOff,Mail,LockKeyhole,Chrome,Apple,Building2,ArrowLeft,CheckCircle2}from'lucide-react';import{useNavigate}from'react-router-dom';import{useAppContext}from'../AppContext.jsx';import WorkspaceShell from'./WorkspaceShell.jsx';
export default function AuthPage(){const{services,configured}=useAppContext();const nav=useNavigate();const[mode,setMode]=useState('signin');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[name,setName]=useState('');const[showPassword,setShowPassword]=useState(false);const[status,setStatus]=useState('');const[busy,setBusy]=useState(false);const fail=e=>setStatus(e?.message||'Authentication failed.');const run=async()=>{try{if(!configured)throw new Error('Authentication is not configured in this deployment.');if(!email||!password)throw new Error('Enter your email and password.');setBusy(true);setStatus(mode==='signin'?'Signing you in…':'Creating your account…');if(mode==='signin'){const{error}=await services.identity.signIn({email,password});if(error)throw error;window.dispatchEvent(new Event('kleenest:auth-refresh'));nav('/profile');}else{const{data,error}=await services.identity.signUp({email,password,fullName:name});if(error)throw error;if(data?.session){window.dispatchEvent(new Event('kleenest:auth-refresh'));nav('/profile');}else setStatus('Account created. Check your email to verify your account, then sign in.')}}catch(e){fail(e)}finally{setBusy(false)}};const magic=async()=>{try{if(!configured)throw new Error('Authentication is not configured in this deployment.');if(!email)throw new Error('Enter your email first.');setBusy(true);setStatus('Sending your secure sign-in link…');const{error}=await services.identity.signInWithMagicLink(email);if(error)throw error;setStatus('Secure sign-in link sent. Check your email.')}catch(e){fail(e)}finally{setBusy(false)}};const oauth=async provider=>{try{if(!configured)throw new Error('Authentication is not configured in this deployment.');setBusy(true);setStatus(`Connecting to ${provider==='azure'?'Microsoft':provider}…`);const{error}=await services.identity.signInWithOAuth(provider);if(error)throw error;}catch(e){fail(e);setBusy(false)}};const reset=async()=>{try{if(!configured)throw new Error('Authentication is not configured in this deployment.');if(!email)throw new Error('Enter your email first.');setBusy(true);setStatus('Sending password reset instructions…');const{error}=await services.identity.sendPasswordReset(email);if(error)throw error;setStatus('Password reset instructions sent. Check your email.')}catch(e){fail(e)}finally{setBusy(false)}};return <WorkspaceShell><main className="auth-shell"><button className="auth-back" onClick={()=>nav('/')}><ArrowLeft size={16}/>Back to Kleenest</button><section className="auth-card"><div className="auth-hero"><div className="auth-logo">K</div><div><span className="eyebrow">KLEENEST ACCOUNT</span><h1>{mode==='signin'?'Sign in to Kleenest':'Create your Kleenest account'}</h1><p>{mode==='signin'?'Your account unlocks personalized maps, check-ins, favorites, progress, business access, and your workspace permissions.':'Start with a free account. Your capabilities can expand as your verified memberships and entitlements change.'}</p></div></div><div className="auth-mode"><button className={mode==='signin'?'active':''} onClick={()=>{setMode('signin');setStatus('')}}>Sign in</button><button className={mode==='signup'?'active':''} onClick={()=>{setMode('signup');setStatus('')}}>Create account</button></div>{mode==='signup'&&<label><span>Name</span><div className="auth-input"><Building2 size={18}/><input value={name} onChange={e=>setName(e.target.value)} autoComplete="name" placeholder="Your name"/></div></label>}<label><span>Email</span><div className="auth-input"><Mail size={18}/><input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email" placeholder="you@example.com"/></div></label>{mode==='signin'&&<button className="auth-magic" type="button" disabled={busy||!configured||!email} onClick={()=>void magic()}><Mail size={17}/><span><strong>Email me a sign-in link</strong><small>No password required</small></span></button>}<label><span>Password</span><div className="auth-input"><LockKeyhole size={18}/><input type={showPassword?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} autoComplete={mode==='signin'?'current-password':'new-password'} placeholder={mode==='signin'?'Your password':'At least 8 characters'}/><button type="button" className="icon-button" aria-label={showPassword?'Hide password':'Show password'} onClick={()=>setShowPassword(v=>!v)}>{showPassword?<EyeOff size={18}/>:<Eye size={18}/>}</button></div></label><button className="auth-submit" type="button" disabled={busy||!configured||!email||!password} onClick={()=>void run()}>{busy?(mode==='signin'?'Signing in…':'Creating account…'):(mode==='signin'?'Sign in':'Create account')}</button>{mode==='signin'&&<div className="auth-links"><button type="button" disabled={busy||!configured||!email} onClick={()=>void reset()}>Forgot password?</button></div>}{mode==='signin'&&<><div className="auth-divider"><span>or continue with</span></div><div className="auth-oauth"><button type="button" disabled={busy||!configured} onClick={()=>void oauth('google')}><Chrome size={17}/>Google</button><button type="button" disabled={busy||!configured} onClick={()=>void oauth('apple')}><Apple size={17}/>Apple</button><button type="button" disabled={busy||!configured} onClick={()=>void oauth('azure')}><Building2 size={17}/>Microsoft</button></div></>}<div className="auth-trust"><CheckCircle2 size={16}/><span>Secure authentication through Supabase. Your membership and workspace permissions load automatically after sign-in.</span></div><div className="auth-status" role="status" aria-live="polite">{status}</div></section></main></WorkspaceShell>}
