import {useState} from 'react';
import {useAppContext} from '../AppContext.jsx';
import WorkspaceShell from './WorkspaceShell.jsx';
export function ProfilePage(){const {profile,services,membershipTier,showAds}=useAppContext();const [form,setForm]=useState({display_name:profile?.display_name??'',username:profile?.username??'',bio:profile?.bio??'',avatar_url:profile?.avatar_url??''});const [status,setStatus]=useState('');const save=async()=>{try{await services.profile.update(profile.id,form);setStatus('Profile saved.')}catch(e){setStatus(e.message)}};return <WorkspaceShell><section className="page-head"><span className="eyebrow">Account</span><h1>Your profile</h1><p>Manage your public identity and see your Kleenest membership.</p><div className="membership-card"><strong>{membershipTier}</strong><span>{showAds?'Ad-supported':'Ad-free'}</span></div>{Object.keys(form).map(k=><label key={k}>{k.replace('_',' ')}<input value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<button className="button primary" onClick={save}>Save profile</button><span>{status}</span></section></WorkspaceShell>}
export function SupportPage(){const {services}=useAppContext();const [form,setForm]=useState({type:'general',subject:'',message:''});const [status,setStatus]=useState('');const submit=async()=>{try{await services.support.create(form);setStatus('Support request submitted.')}catch(e){setStatus(e.message)}};return <WorkspaceShell><section className="page-head"><span className="eyebrow">Help</span><h1>Support</h1><label>Topic<select value={form.type} onChange={e=>setForm({...form,type:e.target.value})}><option value="general">General</option><option value="bug">Bug</option><option value="feature">Feature request</option><option value="account">Account</option><option value="billing">Billing</option></select></label><label>Subject<input value={form.subject} onChange={e=>setForm({...form,subject:e.target.value})}/></label><label>Message<textarea value={form.message} onChange={e=>setForm({...form,message:e.target.value})}/></label><button className="button primary" disabled={!form.message} onClick={submit}>Send support request</button><span>{status}</span></section></WorkspaceShell>}
export function AboutPage(){return <WorkspaceShell><section className="page-head"><span className="eyebrow">Kleenest</span><h1>Better places. Better journeys.</h1><p>Kleenest connects location intelligence, community feedback, routing, rewards, business engagement, fleet operations, and enterprise networks in one system.</p><div className="result-card"><strong>Built around useful actions.</strong><p>Discover, route, verify, contribute, engage, and improve the network.</p></div></section></WorkspaceShell>}
export function LoadingPage(){return <div className="loading-screen" aria-live="polite"><div className="loading-mark" aria-hidden="true">K</div><strong>Kleenest</strong><span>Loading your experience…</span></div>}
