import {useEffect,useState} from 'react';import {BarChart3,Handshake,Megaphone,Pause,Play,RefreshCw,Target,TrendingUp,Wallet,CheckCircle2,Activity,Building2,Users} from 'lucide-react';import {Link} from 'react-router-dom';import {useAppContext} from '../AppContext.jsx';import WorkspaceShell from './WorkspaceShell.jsx';const arr=v=>Array.isArray(v)?v:[];const obj=v=>v&&typeof v==='object'?v:{};const metrics=v=>Object.entries(obj(v)).slice(0,12);const today=()=>new Date().toISOString().slice(0,10);const idOf=v=>v?.business_id||v?.id||'';const nameOf=v=>v?.name||v?.business_name||'Selected business';export default function EnterpriseOperationsPage({mode='partners'}){const{services,selectedBusinessId,selectedBusiness}=useAppContext();const[programs,setPrograms]=useState([]),[memberships,setMemberships]=useState([]),[networkId,setNetworkId]=useState(''),[campaignName,setCampaignName]=useState(''),[campaignType,setCampaignType]=useState('engagement'),[partnerBusinessId,setPartnerBusinessId]=useState(''),[agreementId,setAgreementId]=useState(''),[campaignId,setCampaignId]=useState(''),[report,setReport]=useState({}),[roi,setRoi]=useState({}),[allocation,setAllocation]=useState({}),[outcome,setOutcome]=useState({}),[metric,setMetric]=useState({}),[campaign,setCampaign]=useState(null),[loading,setLoading]=useState(true),[error,setError]=useState(''),[message,setMessage]=useState('');const selectedBusinessName=nameOf(selectedBusiness);const load=async()=>{setLoading(true);setError('');try{const[p,m]=await Promise.all([services.partners.list(),services.partners.memberships()]);setPrograms(arr(p));setMemberships(arr(m));const first=arr(m)[0];setPartnerBusinessId(String(first?.business_id||''));setAgreementId(String(first?.agreement_id||''));if(campaign?.id||campaign?.campaign_id)setCampaignId(String(campaign.id||campaign.campaign_id))}catch(e){setError(e.message||'Unable to load enterprise partner operations.')}finally{setLoading(false)}};useEffect(()=>{void load()},[]);useEffect(()=>{const id=selectedBusinessId||selectedBusiness?.business_id||selectedBusiness?.id||'';setNetworkId(String(id));setCampaign(null);setCampaignId('');setReport({});setRoi({});setAllocation({})},[selectedBusinessId,selectedBusiness]);useEffect(()=>{const id=campaign?.id||campaign?.campaign_id;if(id)setCampaignId(String(id))},[campaign]);useEffect(()=>{const refresh=()=>void load();window.addEventListener('kleenest:business-updated',refresh);window.addEventListener('kleenest:enterprise-updated',refresh);return()=>{window.removeEventListener('kleenest:business-updated',refresh);window.removeEventListener('kleenest:enterprise-updated',refresh)}},[]);const mutate=async(fn,success)=>{setError('');setMessage('');try{const result=await fn();if(result!==undefined)setCampaign(result);setMessage(success);window.dispatchEvent(new CustomEvent('kleenest:enterprise-updated'));await load()}catch(e){setError(e.message||'Enterprise operation failed.')}};const createCampaign=()=>{if(!networkId||!campaignName.trim())return;return mutate(()=>services.enterprise.createCampaign(networkId,campaignName.trim(),campaignType,null),'Partner campaign created.')};const activateCampaign=()=>{const id=campaignId||campaign?.id||campaign?.campaign_id;if(id)return mutate(()=>services.partners.activateCampaign(id),'Partner campaign activated.')};const pauseCampaign=()=>{const id=campaignId||campaign?.id||campaign?.campaign_id;if(id)return mutate(()=>services.partners.pauseCampaign(id),'Partner campaign paused.')};const loadReport=async()=>{if(!networkId)return;setError('');try{const[data,benchmark,allocationRoi]=await Promise.all([services.enterprise.get(networkId),services.enterprise.benchmark(networkId),services.enterprise.allocationRoi(networkId)]);setReport(obj(data));setRoi(obj(benchmark));setAllocation(obj(allocationRoi))}catch(e){setError(e.message||'Unable to load enterprise network data.')}};const loadCampaignRoi=async()=>{const id=campaignId||campaign?.id||campaign?.campaign_id;if(!id)return setError('Create a campaign before loading campaign ROI.');try{setRoi(obj(await services.enterprise.campaignRoi(id)))}catch(e){setError(e.message||'Unable to load campaign ROI.')}};const joinProgram=async id=>mutate(()=>services.partners.join(id),'Partner program joined.');const requestAgreement=async()=>{if(!networkId||!partnerBusinessId)return;await mutate(()=>services.partners.requestAgreement(networkId,partnerBusinessId),'Partner agreement requested.')};const acceptAgreement=async()=>{if(!agreementId)return;await mutate(()=>services.partners.acceptAgreement(agreementId),'Partner agreement accepted.')};const recordOutcome=async()=>{if(!campaignId||!partnerBusinessId)return;await mutate(()=>services.partners.recordCampaignOutcome(campaignId,partnerBusinessId,outcome),'Campaign outcome recorded.')};const recordMetric=async()=>{if(!networkId)return;await mutate(()=>services.partners.recordNetworkMetric(networkId,today(),metric),'Network metric recorded.')};if(loading)return <WorkspaceShell workspace="enterprise"><section className="empty-state">Loading enterprise operations…</section></WorkspaceShell>;return <WorkspaceShell workspace="enterprise"><section className="page enterprise-page"><div className="page-header"><div><span className="eyebrow">ENTERPRISE OPERATIONS</span><h1>{mode==='campaigns'?'Partner campaigns':mode==='performance'?'Network performance':'Partner network'}</h1><p>Enterprise coordinates partner networks, campaign outcomes, allocation efficiency and benchmark intelligence over the canonical business data graph.</p></div><div className="hero-actions"><button className="secondary" onClick={load}><RefreshCw size={16}/>Refresh</button><Link className="secondary" to="/enterprise">Command center</Link></div></div>{error&&<p className="form-error" role="alert">{error}</p>}{message&&<p className="form-success" role="status">{message}</p>}<section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">NETWORK CONTEXT</span><h2>Enterprise network controls</h2></div><Target size={22}/></div><div className="metric-card"><Building2 size={18}/><strong>{selectedBusinessName}</strong><span>Active enterprise network context</span></div>{mode==='campaigns'?<><label className="form-field"><span>Campaign name</span><input value={campaignName} onChange={e=>setCampaignName(e.target.value)} placeholder="Campaign name"/></label><label className="form-field"><span>Campaign type</span><select value={campaignType} onChange={e=>setCampaignType(e.target.value)}><option value="engagement">Engagement</option><option value="promotion">Promotion</option><option value="access">Access</option></select></label><div className="hero-actions"><button className="primary" onClick={createCampaign} disabled={!networkId||!campaignName.trim()}><Megaphone size={16}/>Create campaign</button>{campaign&&(<> <button className="secondary" onClick={activateCampaign}><Play size={16}/>Activate</button><button className="secondary" onClick={pauseCampaign}><Pause size={16}/>Pause</button></>)}</div>{campaign&&<><div className="state success"><CheckCircle2 size={16}/><span><strong>{campaign.name||campaign.title||'Campaign created'}</strong> is now selected for ROI and outcome operations.</span></div><button className="secondary" onClick={loadCampaignRoi} disabled={!campaignId}><TrendingUp size={16}/>Load campaign ROI</button></>}:mode==='performance'?<button className="primary" onClick={loadReport} disabled={!networkId}><BarChart3 size={16}/>Load network intelligence</button>:<div className="hero-actions"><button className="primary" onClick={requestAgreement} disabled={!networkId||!partnerBusinessId}><Handshake size={16}/>Request agreement</button><button className="secondary" onClick={acceptAgreement} disabled={!agreementId}><CheckCircle2 size={16}/>Accept agreement</button></div>}</section>{mode==='partners'&&<><section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">PROGRAMS</span><h2>Available partner programs</h2></div><Handshake size={22}/></div>{programs.length?programs.map(p=><div className="business-row" key={p.id||p.program_id}><div><strong>{p.name||p.program_name||'Partner program'}</strong><span>{p.description||p.status||'Available program'}</span></div><button className="secondary compact" onClick={()=>joinProgram(p.id||p.program_id)}>Join</button></div>):<p className="muted">No partner programs returned.</p>}</section><section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">MEMBERSHIPS</span><h2>My partner memberships</h2></div><Users size={22}/></div>{memberships.length?memberships.map(m=><div className="business-row" key={m.id||m.membership_id}><div><strong>{m.program_name||m.name||'Partner membership'}</strong><span>{m.status||'active'}</span></div></div>):<p className="muted">No memberships yet.</p>}</section></>}{mode==='campaigns'&&<section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">CAMPAIGN OUTCOMES</span><h2>Record partner outcome</h2></div><Activity size={20}/></div><div className="metric-card"><strong>{campaign?.name||campaign?.title||'Create or select a campaign'}</strong><span>{partnerBusinessId?'Partner membership context is attached automatically.':'Join a partner membership to record an outcome.'}</span></div><div className="detail-grid">{[['visits','Visits'],['checkIns','Check-ins'],['reviews','Reviews'],['preferredUses','Preferred uses'],['accessRedemptions','Access redemptions'],['promotionRedemptions','Promotion redemptions'],['attributedUsers','Attributed users'],['pointsAwarded','Points awarded']].map(([key,label])=><label className="form-field" key={key}><span>{label}</span><input type="number" min="0" value={outcome[key]??0} onChange={e=>setOutcome(v=>({...v,[key]:Number(e.target.value)||0}))}/></label>)}</div><button className="primary" onClick={recordOutcome} disabled={!campaignId||!partnerBusinessId}><CheckCircle2 size={16}/>Record outcome</button></section>}{mode==='performance'&&<section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">NETWORK METRICS</span><h2>Record daily network metric</h2></div><Activity size={20}/></div><div className="detail-grid">{[['visits','Visits'],['checkIns','Check-ins'],['reviews','Reviews'],['preferredUses','Preferred uses'],['accessRedemptions','Access redemptions'],['promotionRedemptions','Promotion redemptions']].map(([key,label])=><label className="form-field" key={key}><span>{label}</span><input type="number" min="0" value={metric[key]??0} onChange={e=>setMetric(v=>({...v,[key]:Number(e.target.value)||0}))}</label>)}</div><button className="primary" onClick={recordMetric} disabled={!networkId}><Activity size={16}/>Record metric</button></section>}{Object.keys(report).length>0&&<section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">NETWORK INTELLIGENCE</span><h2>Network report</h2></div><BarChart3 size={20}/></div><pre className="analytics-json">{JSON.stringify(report,null,2)}</pre></section>}{Object.keys(roi).length>0&&<section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">ROI / BENCHMARK</span><h2>Outcome intelligence</h2></div><TrendingUp size={20}/></div><div className="detail-grid">{metrics(roi).map(([k,v])=><div className="metric-card" key={k}><strong>{typeof v==='number'?Number(v.toFixed?.(2)??v):String(v??'—')}</strong><span>{k.replaceAll('_',' ')}</span></div>)}</div></section>}{Object.keys(allocation).length>0&&<section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">ALLOCATION ROI</span><h2>Budget efficiency</h2></div><Wallet size={20}/></div><div className="detail-grid">{metrics(allocation).map(([k,v])=><div className="metric-card" key={k}><strong>{typeof v==='number'?Number(v.toFixed?.(2)??v):String(v??'—')}</strong><span>{k.replaceAll('_',' ')}</span></div>)}</div></section>}</section></WorkspaceShell>};