import { useEffect, useState } from 'react';
import { BarChart3, Handshake, Megaphone, RefreshCw } from 'lucide-react';
import { Link } from 'react-router-dom';
import { useAppContext } from '../AppContext.jsx';
import WorkspaceShell from './WorkspaceShell.jsx';
const arr = v => Array.isArray(v) ? v : [];
const obj = v => v && typeof v === 'object' ? v : {};

export default function EnterpriseOperationsPage({ mode = 'partners' }) {
  const { services } = useAppContext();
  const [programs,setPrograms]=useState([]),[memberships,setMemberships]=useState([]),[networkId,setNetworkId]=useState(''),[campaignName,setCampaignName]=useState(''),[campaignType,setCampaignType]=useState('engagement'),[report,setReport]=useState({}),[campaign,setCampaign]=useState(null),[loading,setLoading]=useState(true),[error,setError]=useState(''),[message,setMessage]=useState('');
  const load=async()=>{setLoading(true);setError('');try{const[p,m]=await Promise.all([services.partners.list(),services.partners.memberships()]);setPrograms(arr(p));setMemberships(arr(m));}catch(e){setError(e.message||'Unable to load enterprise partner operations.')}finally{setLoading(false)}};
  useEffect(()=>{void load()},[]);
  const createCampaign=async()=>{if(!networkId||!campaignName.trim())return;setError('');try{const result=await services.partners.createCampaign(networkId,campaignName.trim(),campaignType);setCampaign(result);setMessage('Partner campaign created.');}catch(e){setError(e.message||'Unable to create partner campaign.')}};
  const loadReport=async()=>{if(!networkId)return;setError('');try{const data=mode==='performance'?await services.partners.benchmarkAnalytics(networkId):await services.partners.network(networkId);setReport(obj(data));}catch(e){setError(e.message||'Unable to load enterprise network data.')}};
  if(loading)return <WorkspaceShell workspace="enterprise"><section className="empty-state">Loading enterprise operations…</section></WorkspaceShell>;
  return <WorkspaceShell workspace="enterprise"><section className="page enterprise-page"><div className="page-header"><div><span className="eyebrow">ENTERPRISE OPERATIONS</span><h1>{mode==='campaigns'?'Partner campaigns':mode==='performance'?'Network performance':'Partner network'}</h1><p>Operational controls use the canonical enterprise partner RPC contracts.</p></div><div className="hero-actions"><button className="secondary" onClick={load}><RefreshCw size={16}/>Refresh</button><Link className="secondary" to="/enterprise">Command center</Link></div></div>{error&&<p className="form-error" role="alert">{error}</p>}{message&&<p className="form-success" role="status">{message}</p>}<section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">NETWORK ID</span><h2>Enterprise network controls</h2></div></div><label className="form-field"><span>Network ID</span><input value={networkId} onChange={e=>setNetworkId(e.target.value)} placeholder="Paste enterprise network ID"/></label>{mode==='campaigns'?<><label className="form-field"><span>Campaign name</span><input value={campaignName} onChange={e=>setCampaignName(e.target.value)} placeholder="Campaign name"/></label><label className="form-field"><span>Campaign type</span><select value={campaignType} onChange={e=>setCampaignType(e.target.value)}><option value="engagement">Engagement</option><option value="promotion">Promotion</option><option value="access">Access</option></select></label><button className="primary" onClick={createCampaign} disabled={!networkId||!campaignName.trim()}><Megaphone size={16}/>Create campaign</button>{campaign&&<pre className="analytics-json">{JSON.stringify(campaign,null,2)}</pre>}</>:<button className="primary" onClick={loadReport} disabled={!networkId}><BarChart3 size={16}/>Load network data</button>}</section>{mode==='partners'&&<><section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">PROGRAMS</span><h2>Available partner programs</h2></div><Handshake size={22}/></div>{programs.length?programs.map(p=><div className="business-row" key={p.id||p.program_id}><div><strong>{p.name||p.program_name||'Partner program'}</strong><span>{p.description||p.status||'Available program'}</span></div><button className="secondary compact" onClick={async()=>{try{await services.partners.join(p.id||p.program_id);setMessage('Partner program joined.');await load()}catch(e){setError(e.message||'Unable to join partner program.')}}}>Join</button></div>):<p className="muted">No partner programs returned.</p>}</section><section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">MEMBERSHIPS</span><h2>My partner memberships</h2></div></div>{memberships.length?memberships.map(m=><div className="business-row" key={m.id||m.membership_id}><strong>{m.program_name||m.name||'Partner membership'}</strong><span>{m.status||'active'}</span></div>):<p className="muted">No memberships yet.</p>}</section></>}{Object.keys(report).length>0&&<section className="detail-panel"><div className="panel-heading"><h2>Network report</h2></div><pre className="analytics-json">{JSON.stringify(report,null,2)}</pre></section>}</section></WorkspaceShell>;
}
