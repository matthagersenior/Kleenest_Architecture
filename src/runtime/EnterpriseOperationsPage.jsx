import {useEffect,useState} from 'react';import {BarChart3,Handshake,Megaphone,Pause,Play,RefreshCw,Target,TrendingUp,Wallet} from 'lucide-react';import {Link} from 'react-router-dom';import {useAppContext} from '../AppContext.jsx';import WorkspaceShell from './WorkspaceShell.jsx';const arr=v=>Array.isArray(v)?v:[];const obj=v=>v&&typeof v==='object'?v:{};const metrics=v=>Object.entries(obj(v)).slice(0,12);
export default function EnterpriseOperationsPage({mode='partners'}){const{services,selectedBusinessId,selectedBusiness}=useAppContext();const[programs,setPrograms]=useState([]),[memberships,setMemberships]=useState([]),[networkId,setNetworkId]=useState(''),[campaignName,setCampaignName]=useState(''),[campaignType,setCampaignType]=useState('engagement'),[report,setReport]=useState({}),[roi,setRoi]=useState({}),[allocation,setAllocation]=useState({}),[campaign,setCampaign]=useState(null),[loading,setLoading]=useState(true),[error,setError]=useState(''),[message,setMessage]=useState('');const load=async()=>{setLoading(true);setError('');try{const[p,m]=await Promise.all([services.partners.list(),services.partners.memberships()]);setPrograms(arr(p));setMemberships(arr(m))}catch(e){setError(e.message||'Unable to load enterprise partner operations.')}finally{setLoading(false)}};useEffect(()=>{void load()},[]);useEffect(()=>{const id=selectedBusinessId||selectedBusiness?.business_id||selectedBusiness?.id;if(id)setNetworkId(String(id))},[selectedBusinessId,selectedBusiness]);useEffect(()=>{const refresh=()=>void load();window.addEventListener('kleenest:business-updated',refresh);window.addEventListener('kleenest:enterprise-updated',refresh);return()=>{window.removeEventListener('kleenest:business-updated',refresh);window.removeEventListener('kleenest:enterprise-updated',refresh)}},[]);const mutate=async(fn,success)=>{setError('');try{const result=await fn();if(result!==undefined)setCampaign(result);setMessage(success);window.dispatchEvent(new CustomEvent('kleenest:enterprise-updated'));await load()}catch(e){setError(e.message||'Enterprise operation failed.')}};const createCampaign=()=>{if(!networkId||!campaignName.trim())return;return mutate(()=>services.enterprise.createCampaign(networkId,campaignName.trim(),campaignType,null),'Partner campaign created.')};const activateCampaign=()=>{const id=campaign?.id||campaign?.campaign_id;if(id)return mutate(()=>services.partners.activateCampaign(id),'Partner campaign activated.')};const pauseCampaign=()=>{const id=campaign?.id||campaign?.campaign_id;if(id)return mutate(()=>services.partners.pauseCampaign(id),'Partner campaign paused.')};const loadReport=async()=>{if(!networkId)return;setError('');try{const[data,benchmark,allocationRoi]=await Promise.all([services.enterprise.get(networkId),services.enterprise.benchmark(networkId),services.enterprise.allocationRoi(networkId)]);setReport(obj(data));setRoi(obj(benchmark));setAllocation(obj(allocationRoi))}catch(e){setError(e.message||'Unable to load enterprise network data.')}};const loadCampaignRoi=async()=>{const id=campaign?.id||campaign?.campaign_id;if(!id)return setError('Create or select a campaign before loading campaign ROI.');try{setRoi(obj(await services.enterprise.campaignRoi(id)))}catch(e){setError(e.message||'Unable to load campaign ROI.')}};const joinProgram=async id=>mutate(()=>services.partners.join(id),'Partner program joined.');if(loading)return <WorkspaceShell workspace="enterprise"><section className="empty-state">Loading enterprise operations…</section></WorkspaceShell>;return <WorkspaceShell workspace="enterprise"><section className="page enterprise-page"><div className="page-header"><div><span className="eyebrow">ENTERPRISE OPERATIONS</span><h1>{mode==='campaigns'?'Partner campaigns':mode==='performance'?'Network performance':'Partner network'}</h1><p>Enterprise coordinates partner networks, campaign outcomes, allocation efficiency and benchmark intelligence over the canonical business data graph.</p></div><div className="hero-actions"><button className="secondary" onClick={load}><RefreshCw size={16}/>Refresh</button><Link className="secondary" to="/enterprise">Command center</Link></div></div>{error&&<p className="form-error" role="alert">{error}</p>}{message&&<p className="form-success" role="status">{message}</p>}<section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">NETWORK CONTEXT</span><h2>Enterprise network controls</h2></div><Target size={22}/></div><label className="form-field"><span>Network ID</span><input value={networkId} onChange={e=>setNetworkId(e.target.value)} placeholder="Enterprise network ID"/></label>{mode==='campaigns'?<><label className="form-field"><span>Campaign name</span><input value={campaignName} onChange={e=>setCampaignName(e.target.value)} placeholder="Campaign name"/></label><label className="form-field"><span>Campaign type</span><select value={campaignType} onChange={e=>setCampaignType(e.target.value)}><option value="engagement">Engagement</option><option value="promotion">Promotion</option><option value="access">Access</option></select></label><div className="hero-actions"><button className="primary" onClick={createCampaign} disabled={!networkId||!campaignName.trim()}><Megaphone size={16}/>Create campaign</button>{campaign&&(<> <button className="secondary" onClick={activateCampaign}><Play size={16}/>Activate</button><button className="secondary" onClick={pauseCampaign}><Pause size={16}/>Pause</button></>)}</div>{campaign&&<><pre className="analytics-json">{JSON.stringify(campaign,null,2)}</pre><button className="secondary" onClick={loadCampaignRoi}><TrendingUp size={16}/>Load campaign ROI</button></>}</>:<button className="primary" onClick={loadReport} disabled={!networkId}><BarChart3 size={16}/>Load network intelligence</button>}</section>{mode==='partners'&&<><section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">PROGRAMS</span><h2>Available partner programs</h2></div><Handshake size={22}/></div>{programs.length?programs.map(p=><div className="business-row" key={p.id||p.program_id}><div><strong>{p.name||p.program_name||'Partner program'}</strong><span>{p.description||p.status||'Available program'}</span></div><button className="secondary compact" onClick={()=>joinProgram(p.id||p.program_id)}>Join</button></div>):<p className="muted">No partner programs returned.</p>}</section><section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">MEMBERSHIPS</span><h2>My partner memberships</h2></div></div>{memberships.length?memberships.map(m=><div className="business-row" key={m.id||m.membership_id}><strong>{m.program_name||m.name||'Partner membership'}</strong><span>{m.status||'active'}</span></div>):<p className="muted">No memberships yet.</p>}</section></>}{Object.keys(report).length>0&&<section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">NETWORK INTELLIGENCE</span><h2>Network report</h2></div><BarChart3 size={20}/></div><pre className="analytics-json">{JSON.stringify(report,null,2)}</pre></section>}{Object.keys(roi).length>0&&<section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">ROI / BENCHMARK</span><h2>Outcome intelligence</h2></div><TrendingUp size={20}/></div><div className="detail-grid">{metrics(roi).map(([k,v])=><div className="metric-card" key={k}><strong>{typeof v==='number'?Number(v.toFixed?.(2)??v):String(v??'—')}</strong><span>{k.replaceAll('_',' ')}</span></div>)}</div></section>}{Object.keys(allocation).length>0&&<section className="detail-panel"><div className="panel-heading"><div><span className="eyebrow">ALLOCATION ROI</span><h2>Budget efficiency</h2></div><Wallet size={20}/></div><div className="detail-grid">{metrics(allocation).map(([k,v])=><div className="metric-card" key={k}><strong>{typeof v==='number'?Number(v.toFixed?.(2)??v):String(v??'—')}</strong><span>{k.replaceAll('_',' ')}</span></div>)}</div></section>}</section></WorkspaceShell>};