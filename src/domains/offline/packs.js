const DB_NAME='kleenest-offline-network';const DB_VERSION=2;const STORES=['packs','locations','businesses','events'];const TTL=24*60*60*1000;function openDb(){return new Promise((resolve,reject)=>{if(typeof indexedDB==='undefined')return reject(new Error('Offline storage is unavailable in this browser.'));const request=indexedDB.open(DB_NAME,DB_VERSION);request.onupgradeneeded=()=>{const db=request.result;STORES.forEach(name=>{if(!db.objectStoreNames.contains(name))db.createObjectStore(name,{keyPath:'id'});});};request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error||new Error('Unable to open offline storage.'));});}async function putMany(store,rows){if(!rows?.length)return;const db=await openDb();await new Promise((resolve,reject)=>{const tx=db.transaction(store,'readwrite');rows.forEach(row=>tx.objectStore(store).put(row));tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error);});db.close();}async function all(store){const db=await openDb();const rows=await new Promise((resolve,reject)=>{const tx=db.transaction(store,'readonly');const request=tx.objectStore(store).getAll();request.onsuccess=()=>resolve(request.result||[]);request.onerror=()=>reject(request.error);});db.close();return rows;}async function markSynced(event){await putMany('events',[{...event,synced_at:new Date().toISOString()}]);}async function replay(client,event){const p=event.payload||{};if(event.event_type==='consumer.check_in')return client.rpc('create_check_in',{p_place_id:p.placeId||p.locationId,p_qr_token:p.qrToken||null});if(event.event_type==='consumer.observation')return client.rpc('submit_restroom_observation',{p_location_id:p.locationId,p_check_in_id:p.checkInId||null,p_observation_type:p.observationType||'condition',p_value:p.value||p.observationValue||{},p_notes:p.notes||null});if(event.event_type==='consumer.arrival'||event.event_type==='consumer.departure'||event.event_type==='consumer.route_started')return client.rpc('record_location_route_event',{p_location_id:p.locationId,p_event_type:event.event_type.split('.').pop(),p_metadata:p.metadata||{offline_replayed:true,client_event_id:event.client_event_id}});if(event.event_type==='fleet.operational_event')return client.rpc('record_fleet_operational_event',{p_business_id:p.businessId,p_event_type:p.eventType||'offline_operational_event',p_payload:{...(p.payload||{}),offline_replayed:true,client_event_id:event.client_event_id}});return{data:null,error:null};}export function createOfflinePackService(client){if(!client)throw new Error('Supabase client is required.');const create=async({packType,name,businessId=null,routeDiscoverySessionId=null,west,south,east,north,expiresHours=24})=>{const{data,error}=await client.rpc('create_offline_pack',{p_pack_type:packType,p_name:name,p_business_id:businessId,p_route_discovery_session_id:routeDiscoverySessionId,p_west:west,p_south:south,p_east:east,p_north:north,p_expires_hours:expiresHours});if(error)throw error;const pack=Array.isArray(data)?data[0]:data;if(pack?.id){const[locations,businesses]=await Promise.all([client.from('offline_pack_locations').select('location_id,snapshot,source_version,cached_at').eq('pack_id',pack.id),client.from('offline_pack_businesses').select('business_id,snapshot,source_version,cached_at').eq('pack_id',pack.id)]);const normalized={...pack,id:String(pack.id),cached_at:new Date().toISOString(),expires_at:pack.expires_at||new Date(Date.now()+expiresHours*3600000).toISOString(),status:pack.status||'ready'};await putMany('packs',[normalized]);await putMany('locations',(locations.data||[]).map(row=>({...row,pack_id:normalized.id,id:`${normalized.id}:location:${row.location_id}`})));await putMany('businesses',(businesses.data||[]).map(row=>({...row,pack_id:normalized.id,id:`${normalized.id}:business:${row.business_id}`})));return normalized;}return pack;};const queue=async({packId,eventType,payload,clientEventId=null})=>{if(!packId)throw new Error('An offline pack is required.');const row={id:crypto.randomUUID(),pack_id:String(packId),event_type:eventType,payload,client_event_id:clientEventId||crypto.randomUUID(),created_at:new Date().toISOString(),synced_at:null};await putMany('events',[row]);return row;};const sync=async()=>{if(typeof navigator!=='undefined'&&!navigator.onLine)return{synced:0,failed:0,pending:0};const pending=(await all('events')).filter(x=>!x.synced_at);let synced=0,failed=0;for(const event of pending){try{const{data,error}=await replay(client,event);if(error)throw error;const{error:recordError}=await client.from('offline_pack_events').upsert({id:event.id,pack_id:event.pack_id,event_type:event.event_type,payload:event.payload,client_event_id:event.client_event_id,created_at:event.created_at,synced_at:new Date().toISOString(),metadata:{authoritative_replay:true,result:data||null}},{onConflict:'client_event_id'});if(recordError)throw recordError;await markSynced(event);synced++;}catch{failed++;}}return{synced,failed,pending:pending.length-synced};};return Object.freeze({create,queue,sync,listCached:()=>all('packs'),cachedLocations:async packId=>(await all('locations')).filter(x=>!packId||String(x.pack_id)===String(packId)),cachedBusinesses:async packId=>(await all('businesses')).filter(x=>!packId||String(x.pack_id)===String(packId)),pending:async()=> (await all('events')).filter(x=>!x.synced_at),freshness:pack=>{if(!pack)return{state:'missing',label:'Not saved'};const expires=new Date(pack.expires_at||0).getTime();if(expires&&expires<=Date.now())return{state:'expired',label:'Expired'};const age=Date.now()-new Date(pack.cached_at||pack.created_at||0).getTime();return age>12*60*60*1000?{state:'stale',label:'Stale'}:{state:'fresh',label:'Fresh'};}});}