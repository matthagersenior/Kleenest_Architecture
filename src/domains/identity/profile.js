const PROFILE_FIELDS=['id','display_name','username','avatar_url','bio','role','subscription_tier','points','level','streak','total_check_ins','total_reviews','is_business_user','is_admin','created_at'];const EDITABLE_FIELDS=['display_name','username','avatar_url','bio'];const PREFERENCE_FIELDS=['profile_visibility','show_activity','show_checkins','show_reviews','allow_followers','discoverable','preferred_units','home_region'];export function createProfileService(client){if(!client)throw new Error('Supabase client is required.');const projection=PROFILE_FIELDS.join(',');const rpc=(name,args={})=>client.rpc(name,args).then(({data,error})=>{if(error)throw error;return data;});return Object.freeze({get:async userId=>{if(!userId)return null;const{data:authData}=await client.auth.getUser();const{data,error}=await client.from('profiles').select(projection).eq('id',userId).maybeSingle();if(error)throw error;if(data)return{...data,email:authData?.user?.email??null};const ensured=await client.rpc('ensure_current_user_profile');if(ensured.error)throw ensured.error;const{data:created,error:createdError}=await client.from('profiles').select(projection).eq('id',userId).maybeSingle();if(createdError)throw createdError;if(!created)return null;return{...created,email:authData?.user?.email??null};},update:async(userId,patch={})=>{if(!userId)throw new Error('User is required.');const payload=Object.fromEntries(EDITABLE_FIELDS.filter(key=>Object.prototype.hasOwnProperty.call(patch,key)).map(key=>[key,patch[key]]));const{data,error}=await client.from('profiles').update(payload).eq('id',userId).select(projection).single();if(error)throw error;const{data:authData}=await client.auth.getUser();return{...data,email:authData?.user?.email??null};},preferences:async()=>rpc('get_my_profile_preferences'),updatePreferences:async(patch={})=>{const payload=Object.fromEntries(PREFERENCE_FIELDS.filter(k=>Object.prototype.hasOwnProperty.call(patch,k)).map(k=>[k,patch[k]]));return rpc('update_my_profile_preferences',{p_preferences:payload});},preferenceFields:Object.freeze(PREFERENCE_FIELDS)});}export{PROFILE_FIELDS,EDITABLE_FIELDS,PREFERENCE_FIELDS};