export function createOperationalIntelligenceConvergenceService({businessIntelligence,fleetIntelligence,intelligenceConvergence}={}){if(!businessIntelligence&&!fleetIntelligence)throw new Error('Operational intelligence requires business or fleet intelligence.');const list=value=>Array.isArray(value)?value:[];const required=value=>{if(!value)throw new Error('Business is required.');return String(value);};return Object.freeze({async snapshot({businessId,surface='business',start=null,end=null}={}){const id=required(businessId);if(surface==='fleet'){const [dashboard,opportunities]=await Promise.all([fleetIntelligence?.dashboard(id),fleetIntelligence?.opportunities(id)]);return{surface,businessId:id,dashboard:dashboard??null,opportunities:list(opportunities)};}const [dashboard,locationIntelligence,roi]=await Promise.all([businessIntelligence?.dashboard(id,start,end),businessIntelligence?.locationIntelligence(id,start,end),businessIntelligence?.roi(id,start,end)]);return{surface,businessId:id,dashboard:dashboard??null,locationIntelligence:list(locationIntelligence),roi:roi??null};},async opportunities({businessId,surface='fleet'}={}){const id=required(businessId);if(surface==='fleet')return list(await fleetIntelligence?.opportunities(id));return list(await businessIntelligence?.locationIntelligence(id));},async recommendations({businessId}={}){const id=required(businessId);if(!fleetIntelligence?.recommendations)return[];return list(await fleetIntelligence.recommendations(id));},async createAction(payload={}){if(!intelligenceConvergence?.createAction)throw new Error('Intelligence action convergence is unavailable.');return intelligenceConvergence.createAction(payload);},async executeAction(actionId){if(!intelligenceConvergence?.executeAction)throw new Error('Intelligence action convergence is unavailable.');return intelligenceConvergence.executeAction(actionId);},async completeAction(actionId,metadata={}){if(!intelligenceConvergence?.completeAction)throw new Error('Intelligence action convergence is unavailable.');return intelligenceConvergence.completeAction(actionId,metadata);},async notify(payload={}){if(!intelligenceConvergence?.notifyFromSignal)throw new Error('Intelligence notification convergence is unavailable.');return intelligenceConvergence.notifyFromSignal(payload);},subscribe(options={}){if(!intelligenceConvergence?.subscribe)return()=>{};return intelligenceConvergence.subscribe(options);},async process(limit=25){if(!intelligenceConvergence?.processJobs)throw new Error('Intelligence job convergence is unavailable.');return intelligenceConvergence.processJobs(limit);}});}