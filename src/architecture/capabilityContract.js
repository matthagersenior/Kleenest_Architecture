import {CAPABILITY_REGISTRY} from './capabilityRegistry.js';
export const CAPABILITY_CONTRACT=Object.freeze(Object.fromEntries(Object.entries(CAPABILITY_REGISTRY).map(([key,value])=>[key,Object.freeze({id:key,label:key.replace(/([A-Z])/g,' $1').replace(/^./,s=>s.toUpperCase()),authority:value.authority||'supabase',services:Object.freeze(value.services||[]),ui:Object.freeze(value.ui||[]),facts:Object.freeze(value.facts||[]),flow:value.flow||null,rule:value.rule||null})])));
export function getCapabilityContract(id){return CAPABILITY_CONTRACT[id]||null;}
export function getCapabilityContractForWorkspace(workspace){return Object.values(CAPABILITY_CONTRACT).filter(cap=>cap.ui.includes(workspace)||cap.ui.includes('all'));}
export function capabilityHasService(id,service){return Boolean(CAPABILITY_CONTRACT[id]?.services.includes(service));}
export function capabilityHasFact(id,fact){return Boolean(CAPABILITY_CONTRACT[id]?.facts.includes(fact));}
export function auditCapabilitySurface({workspace='consumer',services={},visibleCapabilities=[]}={}){const rows=getCapabilityContractForWorkspace(workspace).map(cap=>{const serviceCoverage=cap.services.every(name=>Boolean(services[name]));const visible=visibleCapabilities.length===0||visibleCapabilities.includes(cap.id);return{capability:cap.id,serviceCoverage,visible,complete:serviceCoverage&&visible,services:cap.services,facts:cap.facts};});return{workspace,total:rows.length,complete:rows.filter(row=>row.complete).length,missingServices:rows.filter(row=>!row.serviceCoverage),hidden:rows.filter(row=>!row.visible),rows};}