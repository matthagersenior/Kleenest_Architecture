export const PRODUCT_TIERS=Object.freeze({user:Object.freeze({free:Object.freeze({id:'user_free',label:'Free',ads:true,workspaces:['consumer'],capabilities:['consumer']}),premium:Object.freeze({id:'user_premium',label:'Premium',ads:false,workspaces:['consumer'],capabilities:['consumer','premium']}),fleet:Object.freeze({id:'user_fleet',label:'Fleet User',ads:false,workspaces:['consumer','fleet'],capabilities:['consumer','fleet']}),enterprise:Object.freeze({id:'user_enterprise',label:'Enterprise User',ads:false,workspaces:['consumer','fleet','enterprise'],capabilities:['consumer','fleet','enterprise']})),business:Object.freeze({standard:Object.freeze({id:'business_standard',label:'Business Standard',ads:false,workspaces:['business'],capabilities:['business']}),growth:Object.freeze({id:'business_growth',label:'Business Growth',ads:false,workspaces:['business','enterprise'],capabilities:['business','enterprise']}),fleet:Object.freeze({id:'business_fleet',label:'Business Fleet',ads:false,workspaces:['business','fleet','enterprise'],capabilities:['business','fleet','enterprise']}),enterprise:Object.freeze({id:'business_enterprise',label:'Business Enterprise',ads:false,workspaces:['business','fleet','enterprise'],capabilities:['business','fleet','enterprise']}))});
export const PRODUCT_TIER_IDS=Object.freeze(Object.values(PRODUCT_TIERS).flatMap(group=>Object.values(group).map(tier=>tier.id)));
export function normalizeProductTier(raw='free'){const value=String(raw??'free').toLowerCase().replace(/[\s-]+/g,'_');if(value.includes('business')&&value.includes('enterprise'))return'business.enterprise';if(value.includes('business')&&value.includes('fleet'))return'business.fleet';if(value.includes('business')&&value.includes('growth'))return'business.growth';if(value.includes('business'))return'business.standard';if(value.includes('enterprise'))return'user.enterprise';if(value.includes('fleet'))return'user.fleet';if(value.includes('premium')||value.includes('pro'))return'user.premium';return'user.free';}
export function getProductTier(raw){const key=normalizeProductTier(raw);const [family,tier]=key.split('.');return PRODUCT_TIERS[family]?.[tier]??PRODUCT_TIERS.user.free;}
export function resolveProductModel({membership='free',capabilities=[]}={}){const tier=getProductTier(membership);const caps=new Set([...(tier.capabilities||[]),...(capabilities||[])]);if(caps.has('enterprise')&&String(membership).toLowerCase().includes('business'))return PRODUCT_TIERS.business.enterprise;if(caps.has('fleet')&&String(membership).toLowerCase().includes('business'))return PRODUCT_TIERS.business.fleet;if(caps.has('enterprise'))return PRODUCT_TIERS.user.enterprise;if(caps.has('fleet'))return PRODUCT_TIERS.user.fleet;return tier;}
export function shouldSuppressAds({productTier,entitlements=[]}={}){if(productTier?.ads===false)return true;return (entitlements||[]).some(item=>String(item?.key??item?.feature??item?.code??'').toLowerCase().includes('ads')&&String(item?.value??item?.status??'').toLowerCase()!=='enabled');}
export function capabilityAccess(productTier,capability){return Boolean(productTier?.capabilities?.includes(capability)||productTier?.capabilities?.includes('enterprise'));}
export function getProductFamilies(){return Object.freeze(Object.keys(PRODUCT_TIERS));}