import { createContext, useContext, useEffect, useMemo, useState } from 'react';
import { supabase, requireSupabase } from './infrastructure/supabase/client.js';
import { createIdentityService } from './domains/identity/auth.js';
import { createProfileService } from './domains/identity/profile.js';
import { createIdentitySnapshot } from './domains/identity/identity-snapshot.js';
import { createEntitlementService, evaluateProfileCapabilities, getMembershipTier, shouldShowAds, isPlatformOwner } from './domains/entitlements/access.js';
import { createBillingService } from './domains/billing/catalog.js';
import { createCommerceService } from './domains/billing/commerce.js';
import { createSupportService } from './domains/support/requests.js';
import { createMapNetworkService } from './domains/maps/network.js';
import { createLiveNetworkService } from './domains/live/network.js';
import { createRoutingService } from './domains/routing/route.js';
import { createNotificationInboxService } from './domains/notifications/inbox.js';
import { createNotificationPreferencesService } from './domains/notifications/preferences.js';
import { createCheckInService } from './domains/checkins/checkins.js';
import { createActivityEventService } from './domains/analytics/events.js';
import { createOfflinePackService } from './domains/offline/packs.js';
import { createBusinessManagementService } from './domains/business/management.js';
import { createFleetOperationsService } from './domains/fleet/operations.js';
import { createFleetMetricService } from './domains/fleet/metrics.js';
import { createPartnerProgramService } from './domains/partners/programs.js';
import { createFavoriteService } from './domains/consumer/favorites.js';
import { createReviewService } from './domains/consumer/reviews.js';
import { createLocationEvidenceService } from './domains/consumer/location-evidence.js';
import { createConsumerEngagementService } from './domains/consumer/engagement.js';
import { createAdminOperationsService } from './domains/admin/operations.js';
import { createProgressionService } from './domains/progression/service.js';
import { createFamilyService } from './domains/family/access.js';
const AppContext=createContext(null);
export function AppProvider({children}){const services=useMemo(()=>!supabase?null:Object.freeze({identity:createIdentityService(supabase,{appUrl:path=>`${window.location.origin}${path}`}),profile:createProfileService(supabase),entitlements:createEntitlementService(supabase),billing:createBillingService(supabase),commerce:createCommerceService(supabase),support:createSupportService(supabase),maps:createMapNetworkService(supabase),live:createLiveNetworkService(supabase),routing:createRoutingService(supabase),notifications:createNotificationInboxService(supabase),notificationPreferences:createNotificationPreferencesService(supabase),checkins:createCheckInService(supabase),analytics:createActivityEventService(supabase),offline:createOfflinePackService(supabase),business:createBusinessManagementService(supabase),fleet:createFleetOperationsService(supabase),fleetMetrics:createFleetMetricService(supabase),partners:createPartnerProgramService(supabase),favorites:createFavoriteService(supabase),reviews:createReviewService(supabase),locationEvidence:createLocationEvidenceService(supabase),engagement:createConsumerEngagementService(supabase),admin:createAdminOperationsService(supabase),progression:createProgressionService(supabase),family:createFamilyService(supabase)}),[]);
const [state,setState]=useState({loading:true,user:null,profile:null,entitlements:[],billingEntitlements:[],subscription:null,capabilities:['consumer'],error:null});
useEffect(()=>{let active=true;if(!services){setState(v=>({...v,loading:false}));return undefined;}const load=async(sessionUser=null)=>{try{const user=sessionUser||await services.identity.getCurrentUser();if(!user){if(active)setState({loading:false,user:null,profile:null,entitlements:[],billingEntitlements:[],subscription:null,capabilities:['consumer'],error:null});return;}const [profile,entitlements,subscription,billingEntitlements]=await Promise.all([services.profile.get(user.id),services.entitlements.getCurrentUserEntitlements(),services.billing.getMySubscription(user.id),services.billing.getMyEntitlements(user.id)]);if(active)setState({loading:false,user,profile,entitlements,billingEntitlements,subscription,capabilities:evaluateProfileCapabilities(profile,[...entitlements,...billingEntitlements]),error:null});}catch(error){if(active)setState(v=>({...v,loading:false,error}));}};void load();const {data}=supabase.auth.onAuthStateChange((_event,session)=>void load(session?.user||null));return()=>{active=false;data.subscription.unsubscribe();};},[services]);
const value=useMemo(()=>{const owner=isPlatformOwner(state.profile);const requestedPreview=new URLSearchParams(window.location.search).get('preview');const previewTier=owner&&['free','premium','family','business','fleet','enterprise'].includes(requestedPreview)?requestedPreview:null;const effectiveEntitlements=[...state.entitlements,...state.billingEntitlements];const effectiveMembershipTier=getMembershipTier(state.profile,effectiveEntitlements);return {...state,services,membershipTier:effectiveMembershipTier,presentationTier:previewTier||effectiveMembershipTier,showAds:previewTier?previewTier==='free':shouldShowAds(state.profile,effectiveEntitlements),isPlatformOwner:owner,identitySnapshot:createIdentitySnapshot({user:state.user,profile:state.profile,entitlements:effectiveEntitlements,loading:state.loading,error:state.error}),configured:Boolean(supabase),requireSupabase};},[services,state]);
return <AppContext.Provider value={value}>{children}</AppContext.Provider>}
export function useAppContext(){const context=useContext(AppContext);if(!context)throw new Error('useAppContext must be used inside AppProvider.');return context;}
